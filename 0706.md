# 일일 회고

## 오늘 진행한 작업

이번 단계에서는 `POST` 방식으로 회원가입 기능을 구현했다.

처음에는 단순히 HTML form의 `method`를 `GET`에서 `POST`로 바꾸면 된다고 생각했지만, 실제로는 HTTP 요청을 직접 파싱하는 웹 서버 입장에서 더 많은 처리가 필요하다는 것을 알게 되었다.

이번 작업을 통해 다음 내용을 학습했다.

* HTTP `POST` 요청의 구조
* `Content-Length`를 이용한 request body 읽기
* `application/x-www-form-urlencoded` 형식의 form 데이터 파싱
* `InputStream`, `BufferedInputStream`, `BufferedReader`의 차이
* HTTP header의 대소문자 비구분 규칙
* Path Traversal 보안 취약점과 방어 방법
* WAL, commit, redo, undo, checkpoint 개념
* TCP의 Nagle 알고리즘과 Delayed ACK 개념
* HTTP `Connection` header의 `close`, `keep-alive` 의미

## 새롭게 알게 된 점

### 1. POST 요청은 body를 직접 읽어야 한다

`GET` 방식에서는 form 데이터가 URL 뒤의 query string에 붙는다.

```http
GET /user/create?userId=test&password=1234 HTTP/1.1
```

반면 `POST` 방식에서는 form 데이터가 URL이 아니라 HTTP request body에 담긴다.

```http
POST /user/create HTTP/1.1
Content-Type: application/x-www-form-urlencoded
Content-Length: 29

userId=test&password=1234
```

즉, `POST` 요청을 처리하려면 request line과 header만 읽는 것으로는 부족하고, `Content-Length`를 기준으로 body까지 직접 읽어야 한다.

이번 구현에서는 body에 담긴 `userId=test&password=1234` 형태의 데이터를 읽은 뒤, 기존 query string 파싱 로직을 재사용하여 회원가입 데이터를 처리했다.

### 2. Content-Length는 문자 수가 아니라 바이트 수다

`Content-Length`는 HTTP body의 길이를 나타내지만, 이 값은 문자 수가 아니라 바이트 수다.

이 점이 중요한 이유는 한글과 같은 UTF-8 멀티바이트 문자가 포함될 수 있기 때문이다. 예를 들어 영어 알파벳은 보통 1바이트지만, 한글은 UTF-8에서 보통 3바이트를 사용한다.

따라서 `BufferedReader`처럼 문자 단위로 읽는 도구에서 `Content-Length` 값을 그대로 문자 배열 길이로 사용하면 이론적으로 문제가 생길 수 있다.

더 정확한 방식은 다음 흐름이다.

* `BufferedInputStream`으로 body를 byte 배열로 읽는다.
* `Content-Length`만큼 byte를 모두 누적한다.
* 이후 UTF-8 문자열로 변환한다.

다만 이번 구현에서는 기존 구조를 크게 바꾸지 않기 위해 `BufferedReader` 기반을 유지하되, `read()`를 반복 호출하여 body를 끝까지 읽는 방식으로 처리했다.

### 3. read() 한 번이 body 전체를 읽는다는 보장은 없다

TCP는 데이터의 순서와 신뢰성을 보장하지만, 애플리케이션에서 `read()`를 호출했을 때 HTTP body 전체를 한 번에 넘겨준다는 보장은 없다.

예를 들어 `Content-Length`가 76이어도 첫 번째 `read()`에서는 30만 읽힐 수 있고, 두 번째 `read()`에서 나머지 46이 읽힐 수 있다.

따라서 서버는 다음처럼 처리해야 한다.

* `Content-Length` 값을 확인한다.
* 현재까지 읽은 길이를 누적한다.
* 목표 길이에 도달할 때까지 반복해서 읽는다.

이 부분을 통해 TCP의 신뢰성과 애플리케이션 레벨의 메시지 경계는 별개의 문제라는 점을 이해했다.

### 4. form POST의 Content-Type은 application/x-www-form-urlencoded다

HTML form을 `POST` 방식으로 제출하면 브라우저는 보통 다음 헤더를 자동으로 붙인다.

```http
Content-Type: application/x-www-form-urlencoded
```

이는 body가 다음과 같은 형식으로 인코딩되어 있다는 뜻이다.

```text
key=value&key=value
```

즉, `GET` 요청의 query string과 거의 같은 형식이다.

차이는 다음과 같다.

* `GET`: 데이터가 URL 뒤에 붙는다.
* `POST`: 데이터가 HTTP body에 담긴다.

따라서 기존에 query string을 파싱하던 로직을 `POST` body 파싱에도 재사용할 수 있었다.

### 5. HTTP header 이름은 대소문자를 구분하지 않는다

HTTP header 이름은 대소문자를 구분하지 않는다.

예를 들어 다음 세 헤더는 모두 같은 의미다.

```http
Content-Type: application/x-www-form-urlencoded
content-type: application/x-www-form-urlencoded
CONTENT-TYPE: application/x-www-form-urlencoded
```

따라서 서버에서 `headers.get("Content-Type")`처럼 특정 대소문자에 의존해서 조회하면 문제가 생길 수 있다.

정상적인 요청이더라도 브라우저나 클라이언트가 `content-type`처럼 소문자로 보낼 경우, 서버가 해당 헤더를 찾지 못할 수 있기 때문이다.

이를 방지하기 위해서는 header 이름을 저장할 때 소문자로 정규화하고, 조회할 때도 같은 기준으로 조회하는 방식이 적절하다.

### 6. Path Traversal 취약점도 함께 고려해야 한다

정적 파일을 응답할 때 사용자가 요청한 경로를 그대로 파일 경로에 이어 붙이면 보안 문제가 생길 수 있다.

예를 들어 다음과 같은 요청이 들어올 수 있다.

```text
/../../../../etc/passwd
```

이런 요청을 그대로 파일 경로로 해석하면 정적 파일 루트 바깥의 민감한 파일에 접근할 위험이 있다. 이를 Path Traversal 취약점이라고 한다.

이를 방지하는 방법은 단순히 `..` 문자열을 차단하는 것만으로는 부족할 수 있다. 더 안전한 방식은 다음과 같다.

* 요청 경로를 정적 파일 루트와 결합한다.
* `normalize()`를 통해 의미상의 실제 경로를 계산한다.
* 정규화된 경로가 여전히 `STATIC_ROOT` 내부에 있는지 검증한다.

즉, 핵심은 사용자가 요청한 경로를 신뢰하지 않고, 최종적으로 계산된 파일 경로가 허용된 디렉터리 내부에 있는지 확인하는 것이다.

## 함께 복습한 개념

### 1. WAL, commit, redo, undo, checkpoint

데이터베이스의 WAL은 Write Ahead Log의 약자다.

핵심은 실제 데이터 페이지를 디스크에 반영하기 전에 변경 내용을 먼저 로그에 기록하는 것이다. 이를 통해 장애가 발생해도 로그를 기반으로 복구할 수 있다.

이번에 정리한 개념은 다음과 같다.

* `WAL`: 데이터 변경 내용을 실제 데이터 파일보다 먼저 로그에 기록하는 방식
* `commit`: 트랜잭션의 변경 내용이 WAL에 안전하게 기록되었음을 보장하는 시점
* `redo`: 커밋은 되었지만 실제 데이터 페이지에 아직 반영되지 않은 변경 내용을 다시 적용하는 작업
* `undo`: 커밋되지 않았는데 데이터 페이지에 일부 반영된 변경 내용을 되돌리는 작업
* `checkpoint`: 특정 시점까지의 변경 내용을 데이터 파일에 반영해 두어, 장애 복구 시 다시 확인해야 하는 로그 범위를 줄이는 작업

이를 통해 커밋이 반드시 “데이터 파일까지 즉시 반영되었다”는 뜻은 아니라는 점을 이해했다. 커밋의 핵심은 적어도 WAL에 기록되어 장애가 나도 복구 가능하다는 것이다.

### 2. TCP의 Nagle 알고리즘과 Delayed ACK

TCP의 Nagle 알고리즘은 작은 패킷이 너무 자주 전송되는 것을 줄이기 위한 최적화다.

아직 ACK를 받지 못한 작은 패킷이 있으면, 다음 작은 데이터를 바로 보내지 않고 대기시킨다. 이후 ACK를 받거나 충분한 데이터가 모이면 전송한다.

Delayed ACK는 수신 측 최적화다. 수신 측은 데이터를 받자마자 ACK만 단독으로 보내기보다, 곧 보낼 데이터가 있으면 그 데이터에 ACK를 함께 실어 보내려고 잠깐 기다린다.

문제는 두 최적화가 함께 동작할 때 지연이 발생할 수 있다는 점이다.

* 송신 측은 Nagle 알고리즘 때문에 ACK를 기다린다.
* 수신 측은 Delayed ACK 때문에 다음 데이터가 오기를 기다린다.
* 서로 기다리다가 Delayed ACK 타이머가 만료된 후에야 ACK가 전송된다.

엄밀한 의미의 영구적인 데드락은 아니지만, 짧은 지연이 발생할 수 있다. 이번 복습을 통해 네트워크 최적화 기법이 항상 좋은 결과만 내는 것이 아니라, 상호작용에 따라 지연을 만들 수 있다는 점을 이해했다.

### 3. HTTP Connection header

HTTP의 `Connection` header에는 대표적으로 다음 값이 있다.

```http
Connection: close
```

```http
Connection: keep-alive
```

`close`는 응답 후 TCP 연결을 닫겠다는 의미다. 이 경우 클라이언트는 다음 요청을 보내려면 TCP 연결을 다시 맺어야 한다.

반면 `keep-alive`는 하나의 TCP 연결을 여러 HTTP 요청/응답에 재사용하겠다는 의미다.

이때 중요한 점은 연결을 계속 유지하기 때문에, 클라이언트와 서버가 각 응답 body의 끝을 명확히 알아야 한다는 것이다. 이때 주로 `Content-Length`가 사용된다.

즉, `Content-Length`는 단순히 body 크기를 알려주는 값이 아니라, keep-alive 연결에서 “이번 HTTP 메시지가 어디까지인지”를 구분하는 데에도 중요하다.

## 느낀 점

이번 작업을 통해 `POST` 방식 회원가입 구현은 단순히 form의 method를 바꾸는 작업이 아니라는 것을 알게 되었다.

직접 웹 서버를 구현하는 입장에서는 HTTP 요청을 다음과 같이 명확히 분리해서 처리해야 한다.

* request line
* header
* body

또한 body를 읽을 때는 `Content-Length`를 기준으로 끝까지 읽어야 하고, `Content-Type`을 확인해 body 형식도 해석해야 한다. 여기에 HTTP header의 대소문자 규칙, TCP read의 동작 방식, path traversal 같은 보안 문제까지 함께 고려해야 한다.

이번 단계에서 가장 크게 느낀 점은 웹 서버 구현이 단순한 문자열 처리 문제가 아니라는 것이다. HTTP 프로토콜, TCP 스트림, 파일 시스템 보안, 데이터 인코딩 규칙을 함께 이해해야 안정적인 서버를 만들 수 있다.

특히 직접 구현해보면서 “프레임워크가 대신 처리해주던 것들”이 내부적으로는 꽤 복잡한 규칙 위에서 동작한다는 점을 체감했다. 앞으로 Spring이나 Tomcat 같은 프레임워크를 사용할 때도 단순히 기능을 사용하는 데서 그치지 않고, 내부에서 어떤 방식으로 요청을 읽고 파싱하고 보호하는지 더 의식하면서 공부해야겠다고 느꼈다.

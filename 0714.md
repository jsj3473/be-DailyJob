# 7월 14일 일일 회고

## 오늘 학습한 내용

오늘은 크게 다음 네 가지 주제를 학습했다.

1. 해시와 B-Tree 인덱스
2. 쿼리 처리와 실행 계획
3. 트랜잭션과 동시성 제어
4. 세마포어, 데드락, I/O 다중화

---

## 1. 해시 자료구조와 인덱스

해시 자료구조는 일반적으로 하나의 키를 하나의 값 또는 엔트리에 매핑하는 구조이다.

키를 해시 함수에 입력하면 저장 위치를 결정하는 해시값을 얻을 수 있다. 서로 다른 키가 같은 저장 위치로 매핑되는 상황을 **해시 충돌(Hash Collision)**이라고 한다.

충돌을 해결하는 대표적인 방법은 **체이닝(Chaining)**이다. 같은 버킷에 들어온 엔트리들을 연결 리스트나 트리 등의 자료구조로 연결하는 방식이다.

충돌이 과도하게 발생하면 하나의 버킷에 많은 엔트리가 집중된다. 이 경우 해시 자료구조의 평균적인 `O(1)` 조회 성능을 살리지 못하며, 최악의 경우 조회 시간이 `O(N)`까지 증가할 수 있다.

해시 자료구조는 데이터베이스의 **해시 인덱스**에도 활용할 수 있다.

해시 인덱스는 다음과 같은 점 조회 조건에 유리하다.

```sql
WHERE id = 10
```

반면 키의 정렬 순서를 유지하지 않기 때문에 다음과 같은 범위 검색에는 적합하지 않다.

```sql
WHERE id BETWEEN 10 AND 100
```

정적 해싱은 처음 정한 버킷 수와 공간 구조가 쉽게 변경되지 않는다는 단점이 있다.

데이터가 예상보다 적으면 공간이 낭비되고, 데이터가 예상보다 많으면 충돌과 버킷 오버플로가 빈번하게 발생한다. 실제 데이터 증가량을 정확하게 예측하지 못하면 과소 할당 또는 과도한 할당 문제가 발생할 수 있다.

---

## 2. 인덱스 설계 시 고려할 점

인덱스는 조회 성능을 향상시키지만, 삽입·수정·삭제 시 인덱스도 함께 변경해야 하므로 쓰기 비용을 증가시킨다.

따라서 인덱스를 만들 때는 다음 요소를 고려해야 한다.

* 조회와 쓰기의 비율
* 조건에 맞는 행의 개수
* 컬럼의 선택도
* 범위 검색 여부
* 테이블 크기
* 실제 쿼리 패턴

**선택도(Selectivity)**는 조건을 적용했을 때 전체 행 중 얼마나 적은 행이 선택되는지를 나타내는 개념이다.

조건에 맞는 행이 적을수록 선택도가 높다고 표현한다.

예를 들어 회원 번호처럼 대부분의 값이 고유한 컬럼은 선택도가 높기 때문에 인덱스의 효과가 크다. 반면 성별처럼 값의 종류가 적은 컬럼은 조건을 적용해도 많은 행이 선택되므로 단독 인덱스의 효율이 낮을 수 있다.

다만 선택도가 낮더라도 복합 인덱스의 일부로 사용하거나 특정 쿼리 패턴을 최적화하기 위해 인덱스가 사용될 수 있다.

---

## 3. B-Tree 인덱스와 페이지 I/O

B-Tree 계열 인덱스에서는 일반적으로 하나의 노드가 데이터베이스의 **페이지 또는 블록 단위**로 구성된다.

노드 안에는 여러 개의 키와 자식 페이지를 가리키는 포인터가 저장된다. 하나의 노드에 많은 키를 저장하여 트리의 높이를 낮추기 때문에, 적은 수의 페이지 I/O만으로 원하는 데이터를 찾을 수 있다.

B-Tree 인덱스는 키가 정렬된 상태로 유지되기 때문에 다음 연산에 유리하다.

* 동등 조건 검색
* 범위 검색
* 정렬
* 최솟값과 최댓값 검색
* 접두 컬럼을 활용한 검색

클러스터링 인덱스 또는 클러스터드 인덱스는 테이블의 실제 레코드 배치가 인덱스 키 순서와 밀접하게 연결된 구조이다.

데이터가 키 순서대로 배치되어 있으면 연속된 레코드를 읽을 때 적은 페이지 I/O로 많은 데이터를 가져올 수 있다.

데이터 파일 자체가 정렬된 키 순서를 유지한다면 각 레코드마다 인덱스 엔트리를 만들지 않고, 페이지 또는 일정 레코드 구간마다 인덱스 엔트리를 두는 **희소 인덱스(Sparse Index)**를 구성할 수도 있다.

---

## 4. 쿼리 처리와 관계대수

하나의 SQL 쿼리는 논리적으로 동일한 결과를 반환하는 여러 관계대수 표현식으로 변환될 수 있다.

예를 들어 여러 테이블을 조인하는 쿼리는 조인 순서를 바꿔도 최종 결과가 같을 수 있다.

```text
(A JOIN B) JOIN C
A JOIN (B JOIN C)
(A JOIN C) JOIN B
```

하지만 각 실행 순서에서 생성되는 중간 결과의 크기는 다를 수 있다. 따라서 실제 실행 비용도 달라진다.

쿼리 옵티마이저는 가능한 실행 계획 후보 중에서 예상 비용이 가장 낮은 실행 계획을 선택한다.

옵티마이저가 고려하는 대표적인 요소는 다음과 같다.

* 테이블 접근 순서
* 인덱스 사용 여부
* 조인 순서
* 조인 알고리즘
* 정렬 비용
* 예상 결과 행 수
* 디스크 페이지 I/O
* CPU 및 메모리 비용

---

## 5. 선택과 프로젝션을 먼저 수행하는 이유

관계대수에서는 가능한 경우 선택과 프로젝션을 조인보다 먼저 수행하는 것이 유리하다.

**선택(Selection)**은 조건에 맞는 행만 남기는 연산이다.

```sql
WHERE status = 'ACTIVE'
```

**프로젝션(Projection)**은 필요한 컬럼만 선택하는 연산이다.

```sql
SELECT id, name
```

선택 연산을 조인 전에 수행하면 조인 대상 행의 수를 줄일 수 있다. 프로젝션을 먼저 적용하면 중간 결과가 가지는 각 행의 크기를 줄일 수 있다.

이렇게 중간 결과의 행 수와 행 크기를 줄이면 메모리 사용량과 디스크 블록 전송 횟수를 감소시킬 수 있다.

다만 SQL을 작성한 순서대로 데이터베이스가 실행하는 것은 아니다. 개발자가 작성한 SQL을 기반으로 옵티마이저가 조건 이동, 조인 순서 변경 등의 논리적 최적화를 수행한다.

---

## 6. 디스크 I/O와 순차 접근

데이터베이스 성능에서 중요한 병목 중 하나는 디스크 블록 I/O이다.

같은 개수의 블록을 읽더라도 연속된 블록을 순차적으로 읽는 것이 흩어진 위치의 블록을 랜덤하게 읽는 것보다 일반적으로 저렴하다.

옵티마이저는 다음 비용을 비교하여 전체 테이블 스캔과 인덱스 스캔 중 하나를 선택한다.

```text
전체 블록 순차 읽기 비용
```

```text
인덱스 탐색 비용
+ 인덱스 페이지 접근 비용
+ 실제 레코드 페이지 접근 비용
```

조건에 맞는 행이 많다면 인덱스를 통해 레코드 위치를 하나씩 찾아가는 것보다 테이블 전체를 순차적으로 읽는 것이 더 저렴할 수 있다.

따라서 인덱스가 존재한다고 해서 항상 인덱스를 사용하는 것은 아니다.

---

## 7. 외부 정렬

정렬해야 하는 데이터가 메모리보다 크면 모든 데이터를 한 번에 메모리에 올려 정렬할 수 없다.

이 경우 데이터베이스는 **외부 정렬(External Sort)**을 사용할 수 있다.

기본적인 과정은 다음과 같다.

1. 메모리에 들어갈 수 있는 크기만큼 데이터를 읽는다.
2. 메모리에서 데이터를 정렬한다.
3. 정렬된 결과를 임시 파일에 기록한다.
4. 위 과정을 반복하여 여러 개의 정렬된 런을 만든다.
5. 여러 런을 병합한다.
6. 최종 정렬 결과를 생성한다.

병합 과정에서는 각 런의 일부 블록만 메모리 버퍼에 올린다. 런과 병합 결과는 디스크에 저장되며, 데이터는 블록 단위로 읽고 쓴다.

따라서 외부 정렬의 비용은 CPU 연산보다는 디스크 블록 읽기와 쓰기 횟수에 크게 영향을 받는다.

---

## 8. 조인 알고리즘

오늘은 대표적인 조인 알고리즘의 선택 기준도 학습했다.

### 중첩 루프 조인

한쪽 테이블의 각 행에 대해 다른 테이블을 반복해서 탐색한다.

작은 테이블과 큰 테이블을 조인하거나, 내부 테이블의 조인 컬럼에 인덱스가 있는 경우 효과적일 수 있다.

인덱스를 이용하는 중첩 루프 조인을 **인덱스 중첩 루프 조인**이라고 한다.

### 정렬 병합 조인

두 입력을 조인 키 기준으로 정렬한 뒤, 양쪽을 순차적으로 이동하면서 같은 키를 찾는다.

이미 두 입력이 정렬되어 있거나 정렬 결과를 다른 연산에서도 활용할 수 있다면 유리할 수 있다.

### 해시 조인

한쪽 입력으로 해시 테이블을 만들고, 다른 입력의 조인 키를 이용하여 해시 테이블을 탐색한다.

주로 동등 조인에 적합하다.

```sql
A.id = B.id
```

해시 조인은 조인 키의 순서 관계를 이용하지 않기 때문에 부등호 조건이나 일반적인 범위 조인에는 바로 적용하기 어렵다.

조인 알고리즘은 단순히 “정렬되어 있으면 머지 조인”, “인덱스가 있으면 인덱스 조인”처럼 고정적으로 선택되는 것은 아니다.

옵티마이저는 입력 크기, 정렬 비용, 인덱스 선택도, 메모리 크기, 예상 결과 행 수 등을 함께 비교한다.

---

## 9. 옵티마이저 통계

쿼리 옵티마이저는 실제 데이터를 매번 모두 읽어서 실행 비용을 계산하지 않는다.

대신 테이블과 인덱스에 대해 수집된 통계를 사용한다.

통계에는 다음과 같은 정보가 포함될 수 있다.

* 전체 행 수
* 페이지 수
* 서로 다른 값의 개수
* NULL 비율
* 최솟값과 최댓값
* 값의 분포
* 히스토그램
* 컬럼 간 상관관계

통계가 오래되었거나 데이터 분포를 제대로 반영하지 못하면 옵티마이저가 결과 행 수를 잘못 추정할 수 있다.

잘못된 카디널리티 추정은 비효율적인 조인 순서나 조인 알고리즘 선택으로 이어질 수 있다.

따라서 데이터가 크게 변경된 이후에는 DBMS에 따라 `ANALYZE`와 같은 명령을 사용하여 통계를 갱신할 필요가 있다.

---

## 10. 트랜잭션과 직렬 가능성

트랜잭션은 데이터베이스에서 하나의 논리적 작업 단위를 이루는 연산의 집합이다.

예를 들어 계좌 이체는 다음 연산들이 하나의 트랜잭션을 구성할 수 있다.

1. 출금 계좌 잔액 감소
2. 입금 계좌 잔액 증가
3. 거래 내역 기록

여러 트랜잭션이 동시에 실행되더라도 그 결과가 어떤 직렬 실행 순서와 동일하다면 해당 스케줄을 **직렬 가능(Serializable)**하다고 한다.

직렬 가능성은 동시 실행으로 성능을 얻으면서도 직렬 실행과 같은 결과를 만들기 위한 기준이다.

다만 최종 결과가 같다는 사실만으로 모든 중간 읽기까지 안전했다고 단정해서는 안 된다.

스케줄의 안전성을 판단할 때는 더티 리드, 중간 결과 노출, 복구 가능성 등도 함께 고려해야 한다.

---

## 11. 충돌 직렬 가능성

두 연산이 다음 조건을 만족하면 충돌한다고 한다.

* 서로 다른 트랜잭션에 속한다.
* 같은 데이터 항목에 접근한다.
* 둘 중 하나 이상이 쓰기 연산이다.

따라서 다음 연산들은 서로 충돌하지 않는다.

* 같은 데이터에 대한 `read-read`
* 서로 다른 데이터에 대한 연산

비충돌 연산들의 순서를 서로 바꾸어 직렬 스케줄로 변환할 수 있다면 해당 스케줄을 **충돌 직렬 가능(Conflict Serializable)**하다고 한다.

충돌 직렬 가능성은 선행 그래프를 통해 판별할 수 있다. 트랜잭션 간 선행 관계를 그래프로 표현했을 때 사이클이 없다면 충돌 직렬 가능하다.

---

## 12. Lost Update

Lost Update는 두 트랜잭션이 같은 값을 읽은 뒤 각각 수정하면서 한 트랜잭션의 변경 내용이 사라지는 문제이다.

예를 들어 초기 잔액이 100일 때 다음과 같이 실행될 수 있다.

```text
T1: 잔액 100 읽기
T2: 잔액 100 읽기
T1: 10 증가 후 110 저장
T2: 20 증가 후 120 저장
```

정상적인 결과는 130이어야 하지만 최종 결과가 120이 되면서 T1의 변경 내용이 사라졌다.

Lost Update를 방지하기 위해 다음 방법을 사용할 수 있다.

* 배타 락
* 높은 격리 수준
* 낙관적 락
* 버전 컬럼 검사
* 원자적 갱신 쿼리

```sql
UPDATE account
SET balance = balance + 10
WHERE id = 1;
```

---

## 13. 격리 수준과 DBMS의 역할

개발자는 트랜잭션 또는 세션 단위로 격리 수준을 지정할 수 있다.

대표적인 격리 수준은 다음과 같다.

* Read Uncommitted
* Read Committed
* Repeatable Read
* Serializable

DBMS는 개발자가 요구한 격리 수준을 만족시키기 위해 내부적으로 다양한 동시성 제어 기법을 사용한다.

* 공유 락과 배타 락
* MVCC
* 충돌 감지
* 트랜잭션 대기
* 데드락 탐지
* 트랜잭션 중단과 재시도

즉, 격리 수준은 개발자가 요구하는 동시성 보장 수준이고, 락과 MVCC는 DBMS가 이를 구현하기 위해 사용하는 내부 수단이다.

---

## 14. 세마포어

세마포어는 공유 자원에 동시에 접근할 수 있는 작업의 수를 제한하기 위한 동기화 도구이다.

뮤텍스가 일반적으로 하나의 작업만 임계 구역에 진입하도록 만드는 데 사용된다면, 세마포어는 정해진 개수만큼의 작업을 동시에 허용할 수 있다.

세마포어는 다음과 같은 상황에서 사용할 수 있다.

* 외부 API 동시 호출 수 제한
* 파일 동시 처리 수 제한
* 무거운 연산의 동시 실행 수 제한
* 네트워크 연결 수 제한
* DB 커넥션과 같은 자원 풀 관리
* GPU나 제한된 장비의 사용 개수 제한

세마포어는 단순히 하드웨어 보호만을 위한 것은 아니다. 외부 시스템의 호출 제한을 지키거나, 시스템 과부하와 장애 전파를 방지하는 용도로도 사용한다.

---

## 15. 식사하는 철학자와 라이브락

식사하는 철학자 문제에서는 철학자가 식사하려면 양옆의 포크 두 개를 모두 획득해야 한다.

모든 철학자가 동시에 왼쪽 포크 하나씩을 잡으면 아무도 오른쪽 포크를 얻을 수 없으므로 데드락이 발생한다.

이를 피하기 위해 일정 시간 동안 두 번째 포크를 획득하지 못하면 첫 번째 포크를 내려놓도록 구현할 수 있다.

하지만 모든 철학자가 동일한 시점에 포크를 내려놓고 다시 획득하는 행동을 반복하면 시스템의 상태는 계속 변하지만 실제 작업은 진전되지 않는 **라이브락(Livelock)**이 발생할 수 있다.

라이브락을 피하려면 다음과 같은 방법을 적용할 수 있다.

* 재시도 시간을 무작위화한다.
* 철학자마다 포크 획득 순서를 다르게 한다.
* 동시에 식사할 수 있는 철학자 수를 제한한다.
* 포크를 항상 일정한 전역 순서로 획득한다.

---

## 16. 기아와 데드락 회피

**기아(Starvation)**는 특정 스레드나 프로세스가 계속해서 자원을 획득하지 못하는 상태이다.

다른 작업들이 자원을 계속 선점하거나 우선순위가 높은 작업만 실행되면 특정 작업이 무한정 대기할 수 있다.

데드락과 기아는 다르다.

* 데드락: 여러 작업이 서로가 가진 자원을 기다려 모두 멈춘다.
* 기아: 시스템은 계속 동작하지만 특정 작업만 실행되지 못한다.
* 라이브락: 작업들의 상태는 변하지만 실질적인 진행이 없다.

데드락 회피의 대표적인 방법으로 **은행원 알고리즘**이 있다.

은행원 알고리즘은 프로세스들의 최대 자원 요구량과 현재 할당 상태를 바탕으로, 자원을 추가로 할당해도 모든 프로세스가 종료 가능한 안전 상태인지 시뮬레이션한다.

안전 상태가 유지되는 경우에만 자원을 할당하여 데드락 가능성을 회피한다.

다만 실제 범용 운영체제에서는 프로세스의 최대 자원 요구량을 미리 정확히 알기 어렵기 때문에 제한적인 환경이나 이론적 설명에서 주로 다뤄진다.

---

## 17. I/O 다중화와 epoll

`select`, `poll`, `epoll`과 같이 여러 I/O 대상을 하나의 실행 흐름에서 감시하는 방식을 **I/O 다중화(I/O Multiplexing)**라고 한다.

각 연결마다 별도의 스레드를 블로킹시키는 대신, 여러 소켓의 준비 상태를 하나의 스레드가 감시할 수 있다.

리눅스의 `epoll`은 감시할 파일 디스크립터를 커널에 등록한다.

커널은 등록된 파일 디스크립터 중 다음과 같은 I/O가 가능한 상태가 된 대상을 관리한다.

* 읽을 데이터가 도착한 소켓
* 쓰기가 가능한 소켓
* 새로운 연결이 도착한 서버 소켓
* 오류나 연결 종료가 발생한 소켓

애플리케이션은 `epoll_wait()`를 호출하여 준비된 파일 디스크립터 목록을 전달받는다.

중요한 점은 `epoll`이 실제 비즈니스 로직이나 데이터 처리를 대신 수행하는 것이 아니라, **어떤 파일 디스크립터에서 I/O를 수행할 수 있는지 알려주는 준비 상태 통지 메커니즘**이라는 것이다.

이후 실제 `read`, `write`, `accept` 처리는 사용자 프로세스가 수행한다.

---

## 오늘의 핵심 정리

오늘 학습한 내용들은 서로 분리된 주제처럼 보이지만, 모두 제한된 자원을 효율적으로 사용하는 방법과 연결되어 있다.

인덱스는 디스크 I/O를 줄이기 위한 자료구조이고, 쿼리 옵티마이저는 여러 실행 방법 중 예상 자원 비용이 가장 낮은 방법을 선택한다.

트랜잭션 동시성 제어는 여러 작업을 동시에 처리하면서도 데이터의 정합성을 유지하기 위한 기술이다.

세마포어는 동시에 사용할 수 있는 자원의 수를 제한하고, `epoll`은 하나의 스레드가 많은 네트워크 연결을 효율적으로 감시할 수 있도록 한다.

결국 오늘 공부한 핵심은 다음 문장으로 정리할 수 있다.

> 데이터베이스와 운영체제는 제한된 CPU, 메모리, 디스크, 네트워크 자원을 효율적으로 사용하면서도 정확성과 동시성을 보장하기 위해 다양한 자료구조와 스케줄링 전략을 사용한다.

---

## 학습하면서 새롭게 정리된 점

기존에는 인덱스를 단순히 “조회 속도를 빠르게 만드는 자료구조”로 이해했다.

오늘은 인덱스 사용 여부가 단순히 존재 여부로 결정되지 않고, 선택도와 랜덤 I/O 비용, 실제 레코드 접근 비용, 전체 테이블 스캔 비용을 비교하여 결정된다는 점을 학습했다.

또한 SQL 쿼리는 작성된 문장 그대로 실행되는 것이 아니라, 여러 관계대수 표현과 실행 계획 후보로 변환된다는 것을 알게 되었다.

트랜잭션에서는 단순히 여러 연산을 묶는 것뿐만 아니라, 동시에 실행되는 트랜잭션들의 스케줄이 직렬 실행과 동등한지를 판단하는 것이 중요했다.

운영체제 부분에서는 데드락, 라이브락, 기아를 구분할 수 있게 되었고, 세마포어가 단순한 이론적 동기화 도구가 아니라 외부 API 호출 제한이나 무거운 작업의 동시 실행 수 제한과 같은 실제 서버 개발에도 활용된다는 점을 이해했다.

---

## 추가로 공부할 내용

다음 학습에서는 아래 내용을 더 깊게 확인해 보고 싶다.

* 실행 계획에서 카디널리티가 계산되는 방식
* PostgreSQL의 `EXPLAIN`과 `EXPLAIN ANALYZE`
* Nested Loop Join, Hash Join, Merge Join의 실제 비용 비교
* MVCC에서 버전 데이터가 관리되는 방식
* 격리 수준별로 발생 가능한 이상 현상
* 선행 그래프를 이용한 충돌 직렬 가능성 판별
* 세마포어와 커넥션 풀의 관계
* `select`, `poll`, `epoll`의 구현 및 성능 차이
* Level Trigger와 Edge Trigger의 차이
* 비동기 I/O와 I/O 다중화의 차이

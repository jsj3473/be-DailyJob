# 일일 회고 — 실시간 위치 처리와 MySQL 쓰기 최적화

오늘은 퀵서비스 프로젝트에서 다수 라이더의 위치 정보를 짧은 주기로 처리할 때 발생할 수 있는 부하와 이를 줄이기 위한 구조를 학습했다. 핵심은 MySQL 자체를 미세하게 튜닝하기 전에, 애플리케이션에서 불필요한 DB 접근을 줄이고 데이터의 성격에 맞게 저장소를 분리하는 것이다.

프로젝트의 배포 환경은 웹 서버와 DB 서버가 각각 `t3.micro` 또는 `t4g.micro` 수준으로 제한된다. 두 인스턴스 모두 2 vCPU와 1GB 메모리를 제공하지만, T 계열은 CPU 크레딧을 사용하는 버스트형 인스턴스다. 따라서 짧은 부하에서는 높은 성능을 낼 수 있어도 CPU 사용률이 장시간 높게 유지되면 성능 저하나 추가 비용이 발생할 수 있다. 또한 웹 서버와 MySQL 모두 1GB 메모리 환경이므로 JVM Heap, Tomcat 스레드, MySQL Buffer Pool, DB 커넥션 수를 보수적으로 설정해야 한다.

성능 테스트 시나리오는 정합성 테스트와 분리해야 한다. 라이더 위치 갱신, 주변 배송요청 조회, SSE 위치 전달은 처리량, 응답시간, CPU, 메모리, 네트워크 사용량을 측정하는 성능 테스트 대상이다. 반면 여러 라이더가 동일 주문을 동시에 수락하는 배차 동시성은 하나의 라이더만 성공하는지를 확인하는 정합성 테스트가 핵심이다. 락 대기 시간이나 조건부 UPDATE 성능은 부가 지표로 볼 수 있지만, 일반적인 부하 테스트와는 구분하는 것이 적절하다.

고객에게 라이더 위치를 전달하는 방식으로 Polling과 SSE를 비교했다. Polling은 구현이 단순하지만 위치가 변하지 않아도 주기적으로 요청이 발생하고, 실시간성과 요청량 사이에 상충 관계가 있다. SSE는 하나의 HTTP 연결을 유지하면서 서버가 위치 변경 이벤트를 전달하므로 단방향 실시간 통신에 적합하다. 현재는 단일 WAS이므로 위치를 수신한 Spring Boot가 같은 프로세스에서 SSE 구독자에게 직접 전달하면 된다. Redis Pub/Sub은 여러 WAS 사이에서 이벤트를 공유해야 할 때 의미가 크므로 현재 단계에서는 필수가 아니다.

Redis는 실시간 위치를 저장하기 위해 반드시 필요한 것은 아니다. 단일 서버라면 `ConcurrentHashMap` 같은 JVM 메모리 구조에 최신 위치를 둘 수도 있다. 그러나 이번 프로젝트에서는 대규모 주변 검색 테스트, 거리 검색 구현 비용 절감, 세션 공유, 향후 다중 서버 확장 가능성을 고려해 Redis를 사용하기로 했다. Redis GEO에는 라이더 ID와 최신 좌표를 저장하고, 특정 위치를 기준으로 반경 내 라이더를 빠르게 검색한다. 다만 Redis GEO가 계산하는 것은 직선거리 기반 후보 검색이며, 실제 도로 거리나 ETA는 지도 API가 담당해야 한다. 라이더 상태, 차량 종류, 마지막 갱신 시각 등은 별도의 Hash 등에 저장해야 한다.

위치 데이터는 역할에 따라 분리한다. Redis에는 라이더의 최신 위치만 덮어쓰고, MySQL에는 장기적으로 필요한 위치 이력만 저장한다. 모든 위치 수신값을 MySQL에 기록하지 않고 일정 거리 이상 이동했거나 일정 시간이 지난 경우 등 저장 조건을 만족한 데이터만 선별한다. 이렇게 하면 행 탐색, 락, Undo Log, Redo Log, Dirty Page, 인덱스 수정, 디스크 Flush 비용이 모두 감소한다.

MySQL 이력 저장은 Spring Batch가 아니라 JDBC Batch를 사용한다. Spring Batch는 정산, 통계, 대용량 파일 처리처럼 Job과 Step, 재시작 및 실행 이력을 관리하는 정기 작업 프레임워크다. 실시간으로 들어오는 위치를 짧게 모아 저장하는 구조는 JVM 메모리 큐와 JDBC Batch의 조합이 적합하다. Spring Boot가 저장 대상 위치를 제한된 크기의 메모리 큐에 넣고, 별도 소비자 스레드가 50~100건 또는 1초 단위로 꺼내 하나의 짧은 트랜잭션에서 MySQL에 Batch INSERT한다. Redis Stream은 사용하지 않는다. 다만 서버 장애나 배포 시 아직 저장하지 않은 위치 이력은 유실될 수 있으므로, 일부 이력 유실을 허용한다는 전제가 필요하다. 큐 크기 제한, 짧은 Flush 주기, 종료 시 남은 데이터 저장 같은 보완도 필요하다.

InnoDB에서 UPDATE나 INSERT가 실행되면 대상 레코드를 찾고 락을 획득한 뒤, 이전 버전을 Undo Log에 기록하고 Buffer Pool의 페이지를 수정한다. 변경 내용은 Redo Log에 기록되며, 커밋 시 데이터 페이지 전체를 즉시 쓰기보다 Redo Log의 내구성을 먼저 확보한다. 수정된 Dirty Page는 이후 백그라운드에서 데이터 파일로 Flush된다. 코드 레벨에서 각 단계를 직접 제어하기보다는 이 경로를 통과하는 데이터의 수를 줄이는 것이 가장 효과적인 최적화다.

애플리케이션에서 중요한 최적화는 세 가지다. 첫째, 불필요한 쓰기를 제거한다. 둘째, 필요한 쓰기는 JDBC Batch로 적절히 묶는다. 셋째, 트랜잭션을 짧게 유지한다. 트랜잭션 안에서 외부 지도 API 호출, SSE 전송, 긴 계산을 수행하면 DB 락과 커넥션을 오래 점유하므로 DB 작업만 트랜잭션 안에 포함해야 한다. Buffer Pool 효율은 개발자가 직접 조작하는 것이 아니라, 자주 접근하는 데이터 집합을 작게 유지하고 적절한 PK와 최소한의 인덱스를 사용하는 설계로 간접적으로 높인다.

오토커밋과 그룹 커밋도 구분했다. 오토커밋은 SQL 한 문장을 하나의 트랜잭션으로 자동 종료할지 결정하는 설정이다. 그룹 커밋은 여러 트랜잭션이 비슷한 시점에 커밋될 때 MySQL이 Redo Log의 디스크 동기화 비용을 내부적으로 묶어 처리하는 최적화다. 오토커밋 상태에서도 그룹 커밋은 동작할 수 있지만 트랜잭션 수 자체가 줄어드는 것은 아니다. 따라서 그룹 커밋에 의존하기보다 위치 이력을 JDBC Batch와 하나의 적절한 트랜잭션으로 묶는 것이 우선이다.

최종적으로 정리한 위치 처리 흐름은 다음과 같다.

```text
라이더 위치 전송
→ Spring Boot에서 좌표와 생성 시각 검증
→ Redis GEO에 최신 위치 갱신
→ 고객 SSE 연결에 직접 전달
→ 거리·시간 기준으로 이력 저장 여부 판단
→ 저장 대상이면 JVM 메모리 큐에 적재
→ 일정 건수 또는 시간이 되면 JDBC Batch INSERT
→ MySQL에 장기 위치 이력 저장
```

오늘의 핵심 결론은 **MySQL을 빠르게 만드는 것보다 MySQL에 불필요한 데이터를 보내지 않는 것이 먼저**라는 점이다. Redis는 최신 위치와 주변 검색, 세션을 담당하고, MySQL은 선별된 장기 이력을 담당한다. 필요한 DB 쓰기는 적절히 배치하고 트랜잭션을 짧게 유지하는 것이 제한된 인프라에서 가장 효과적인 최적화 방향이다.

# Claude Code 사용을 위한 컨텍스트·토큰 관리 정리

## 1. LLM과 토큰

토큰은 LLM이 입력과 출력을 처리하는 기본 단위다. 하나의 토큰이 반드시 한 단어나 한 글자와 대응하는 것은 아니며, 단어의 일부·공백·문장부호 등이 각각 토큰이 될 수 있다.

에이전트형 LLM의 일반적인 실행 흐름은 다음과 같다.

```text
사용자 요청
→ LLM의 상황 판단
→ 필요한 도구 호출
→ 도구 결과 반환
→ LLM의 재판단
→ 추가 도구 호출 또는 최종 응답
```

도구가 필요한 작업에서는 이러한 판단과 호출이 여러 번 반복될 수 있다.

## 2. 멀티턴 대화와 토큰 비용

멀티턴 대화에서는 일반적으로 이전 대화 내용이 다음 요청의 컨텍스트에 포함된다.

다음 내용이 후속 턴의 입력에 포함될 수 있다.

* 이전 사용자 메시지
* 이전 모델 응답
* 시스템 지침과 프로젝트 지침
* 도구 정의
* 도구 호출 내용
* 도구 실행 결과
* 읽은 파일이나 검색 결과

따라서 모델이 이전 턴에 생성한 출력도 다음 턴에서는 입력 토큰의 일부가 될 수 있다.

대화가 길어질수록 각 턴의 입력 토큰은 대체로 선형적으로 증가한다. 전체 세션에서 사용된 누적 입력 토큰은 여러 턴의 컨텍스트가 반복 처리되므로 이차적으로 증가할 수 있다. 이를 단순히 기하급수적으로 증가한다고 표현하는 것은 정확하지 않다.

API 비용은 일반적으로 다음 항목을 기준으로 계산된다.

* 일반 입력 토큰
* 출력 토큰
* 프롬프트 캐시 쓰기 토큰
* 프롬프트 캐시 읽기 토큰
* 검색 등 별도 과금 도구

도구를 많이 호출하면 모델 호출 횟수와 컨텍스트가 증가할 수 있다. 특히 도구가 긴 검색 결과, 로그, 파일 내용을 반환하면 토큰 사용량이 크게 증가한다.

## 3. 긴 컨텍스트의 문제

긴 컨텍스트는 비용뿐 아니라 응답 품질에도 영향을 줄 수 있다.

컨텍스트 윈도우 안에 정보가 들어 있다고 해서 모델이 모든 정보를 동일한 정확도로 활용하는 것은 아니다. 대화가 지나치게 길어지면 다음 문제가 발생할 수 있다.

* 오래된 지시와 현재 지시의 충돌
* 이미 해결된 문제에 대한 정보가 계속 남음
* 중요한 정보가 불필요한 정보에 묻힘
* 대화 중간의 세부사항을 놓침
* 관련 없는 파일이나 로그를 참고함
* 응답 지연과 비용 증가

따라서 무조건 긴 대화를 유지하기보다 현재 작업에 필요한 핵심 정보만 보존하는 것이 유리할 수 있다.

## 4. 대표적인 컨텍스트 관리 기법

### 컨텍스트 압축

기존 대화를 요약하고 핵심 결정 사항만 남긴다.

단순 요약보다 다음 항목을 명시적으로 보존하는 것이 중요하다.

* 현재 목표
* 완료된 작업
* 변경된 파일
* 중요한 설계 결정
* 해결되지 않은 문제
* 반드시 지켜야 할 제약
* 다음 작업

Claude Code에서는 `/compact`를 사용할 수 있다.

```text
/compact 인증 구조, 변경된 파일, 미해결 오류와 다음 작업을 보존해줘
```

### 슬라이딩 윈도우

최근 N개의 메시지만 유지하고 오래된 대화를 제거하는 방식이다.

단순하지만 오래된 핵심 결정까지 사라질 수 있으므로, 중요한 정보는 별도 메모리나 문서에 보존해야 한다.

### RAG

필요한 문서나 코드만 검색하여 현재 컨텍스트에 넣는 방식이다.

전체 코드베이스나 전체 문서를 항상 제공하지 않고, 현재 질문과 관련된 부분만 가져오므로 컨텍스트 사용량을 줄일 수 있다.

다만 검색이 잘못되면 필요한 정보를 가져오지 못할 수 있으므로 문서 구조, 파일명, 검색 단위가 중요하다.

### 프롬프트 캐싱

반복되는 시스템 지침, 도구 정의, 프로젝트 지침과 같은 공통 prefix를 캐싱하여 반복 처리 비용과 지연을 줄이는 방식이다.

캐싱은 해당 토큰을 없애는 것이 아니라, 동일한 prefix가 재사용될 때 더 저렴한 캐시 읽기 요율을 적용하는 최적화에 가깝다.

## 5. Claude Code의 컨텍스트 최적화

Claude Code는 일반적인 단순 챗봇보다 코드 작업에 필요한 에이전트 실행 구조를 제공한다.

주요 최적화 기능은 다음과 같다.

* 프롬프트 캐싱
* 컨텍스트 한도에 가까워졌을 때의 자동 압축
* `/compact`를 통한 수동 압축
* 세션 저장 및 재개
* 필요한 작업을 별도 서브에이전트에 위임
* Tool Search를 이용한 도구 지연 탐색
* Skills를 통한 필요 시점의 지침 로딩

Claude Code도 이전 대화와 작업 결과를 컨텍스트로 사용하므로 세션이 길어질수록 비용과 노이즈가 증가할 수 있다. 다만 항상 원본 대화 전체를 아무런 최적화 없이 그대로 전송한다고 표현하는 것은 부정확하다. 자동 압축과 캐싱 등의 최적화가 적용될 수 있기 때문이다.

## 6. 세션 관리 명령

### `/clear`

현재 작업과 관계없는 새로운 작업으로 전환할 때 사용한다.

기존 세션의 오래된 컨텍스트를 계속 유지하면 이후 요청에서도 불필요한 토큰이 소비된다.

```text
/clear
```

사용하기 적합한 경우:

* 완전히 다른 기능을 개발할 때
* 기존 버그 수정과 무관한 작업으로 넘어갈 때
* 오래된 지시가 현재 작업을 방해할 때
* 컨텍스트가 지나치게 혼잡해졌을 때

나중에 다시 돌아올 작업이라면 `/rename`으로 세션 이름을 지정한 뒤 `/resume`으로 복귀할 수 있다.

### `/compact [지시문]`

현재 작업은 이어가되 대화가 지나치게 길어졌을 때 사용한다.

```text
/compact 현재 아키텍처, 변경 파일, 테스트 결과와 남은 작업을 보존해줘
```

압축할 때 보존할 내용을 구체적으로 지시하는 것이 좋다.

### `/rewind`

체크포인트를 기준으로 대화 상태나 Claude가 수행한 파일 변경을 되돌릴 때 사용한다.

다만 `/rewind`는 Git을 대체하지 않는다. 중요한 변경 전에는 별도 브랜치나 커밋을 만들어야 한다.

### `/resume`

이전에 저장된 세션으로 돌아가 작업을 이어갈 때 사용한다.

하나의 작업이 며칠 동안 이어진다면 세션을 무한정 유지하기보다 다음과 같이 관리하는 것이 좋다.

1. 작업 단위별로 세션 이름을 지정한다.
2. 작업 종료 시 핵심 상태를 문서나 이슈에 남긴다.
3. 필요하면 `/compact`로 세션을 정리한다.
4. 이후 `/resume`으로 해당 세션을 재개한다.

## 7. MCP

MCP(Model Context Protocol)는 LLM 애플리케이션이 외부 시스템의 데이터와 도구를 일정한 규격으로 사용할 수 있게 하는 개방형 프로토콜이다.

예를 들어 다음과 같은 MCP 서버를 연결할 수 있다.

* GitHub
* Jira
* Slack
* Google Drive
* 데이터베이스
* 사내 API
* 파일 시스템
* 브라우저 및 검색 도구

구조를 단순화하면 다음과 같다.

```text
Claude Code
→ MCP 클라이언트
→ MCP 서버
→ GitHub·DB·Slack 등의 외부 시스템
```

MCP 서버는 모델 자체의 학습 지식을 갱신하는 것이 아니다. 모델이 실행 시점에 외부 데이터를 조회하거나 외부 작업을 수행할 수 있도록 연결하는 수단이다.

MCP 도구가 많으면 도구 정의와 검색 공간이 컨텍스트 및 모델 판단에 부담을 줄 수 있다. 현재 Claude Code는 Tool Search를 이용해 필요한 도구를 동적으로 탐색할 수 있지만, 환경이나 설정에 따라 도구 정의가 미리 로딩될 수도 있다.

따라서 다음 원칙을 적용하는 것이 좋다.

* 실제로 사용하는 MCP 서버만 활성화한다.
* 역할이 겹치는 MCP 서버를 여러 개 연결하지 않는다.
* 한 번에 지나치게 많은 결과를 반환하지 않게 한다.
* 서버별 도구 이름과 설명을 명확하게 작성한다.
* 작업별로 필요한 MCP만 사용한다.
* 민감한 작업은 권한과 허용 도구를 제한한다.

## 8. CLAUDE.md의 역할

`CLAUDE.md`는 Claude Code에 프로젝트의 지속적인 지침과 배경정보를 제공하는 Markdown 파일이다.

주로 다음 정보를 넣는다.

* 프로젝트의 목적과 구조
* 주요 실행 및 테스트 명령
* 아키텍처 원칙
* 디렉터리별 책임
* 코딩 규칙
* 사용해야 하는 라이브러리
* 피해야 하는 구현 방식
* PR 및 리뷰 기준
* 개발 시 고려해야 할 맥락

예시:

```markdown
# Project overview

Spring Boot 기반 공유 자전거 대여 서비스다.

# Commands

- Test: `./gradlew test`
- Run: `./gradlew bootRun`

# Architecture

- Controller는 요청 검증과 응답 변환만 담당한다.
- 비즈니스 규칙은 Service에 둔다.
- Repository 밖에서 JPA EntityManager를 직접 사용하지 않는다.

# Working guidelines

- 기존 구현을 먼저 읽고 수정 계획을 제시한다.
- 공개 API가 변경되면 관련 테스트와 문서를 함께 수정한다.
- 불필요한 신규 라이브러리를 추가하지 않는다.
```

`CLAUDE.md`는 시스템 프롬프트와 유사한 프로젝트 지침 역할을 하지만, 절대적으로 강제되는 정책 엔진은 아니다. 지시가 모호하거나 충돌하면 모델이 일관되게 따르지 못할 수 있다.

따라서 CLAUDE.md에는 다음과 같은 유동적인 지침을 두는 것이 적합하다.

* 설계 방향
* 선호하는 구현 방식
* 작업 순서
* 프로젝트 배경
* 코드 리뷰 기준
* 일반적인 예외 처리 원칙

## 9. Hooks의 역할

Hooks는 Claude Code의 특정 실행 시점에 자동으로 명령, HTTP 요청 또는 검증 로직을 실행하는 기능이다.

다음과 같이 반드시 실행되어야 하는 규칙에 적합하다.

* 파일 수정 후 formatter 실행
* 커밋 전에 lint 실행
* 특정 파일 수정 금지
* 위험한 셸 명령 차단
* 테스트 실패 시 작업 중단
* 특정 디렉터리에 대한 접근 통제
* 도구 호출 전후의 감사 로그 기록

구분하면 다음과 같다.

```text
유동적인 개발 가이드
→ CLAUDE.md

필요할 때 불러오는 반복 절차
→ Skill

반드시 자동으로 실행하거나 검증할 규칙
→ Hook, linter, test, CI, permission
```

Claude가 반드시 지켜야 하는 규칙을 자연어 지시만으로 강제하려 해서는 안 된다.

## 10. CLAUDE.md 파일 구조

작은 프로젝트에서는 루트에 하나의 `CLAUDE.md`만 두는 것으로 충분하다.

모듈별 `CLAUDE.md`는 다음 조건에서 유용하다.

* 모노레포
* 프론트엔드와 백엔드의 규칙이 명확히 다름
* 모듈별 실행 및 테스트 방법이 다름
* 특정 하위 디렉터리에만 적용되는 규칙이 많음
* 루트 파일이 지나치게 길어짐

5주 규모의 소규모 프로젝트라면 처음부터 모든 모듈에 별도 파일을 두는 것은 관리 비용이 더 클 가능성이 높다.

권장 방식은 다음과 같다.

1. 프로젝트 루트에 하나의 `CLAUDE.md`를 둔다.
2. 반복되는 절차가 길어지면 Skill로 분리한다.
3. 실제로 서로 다른 규칙이 생긴 모듈에만 하위 `CLAUDE.md`를 추가한다.
4. 설명이 아니라 강제가 필요한 규칙은 Hook이나 CI로 옮긴다.

## 11. 디렉터리 구조

기능 또는 도메인 단위로 관련 코드를 모으는 구조는 최근 애플리케이션 개발에서 많이 사용된다.

예:

```text
src/main/java/com/example/bikeshare
├── member
│   ├── MemberController.java
│   ├── MemberService.java
│   ├── MemberRepository.java
│   └── Member.java
├── rental
│   ├── RentalController.java
│   ├── RentalService.java
│   ├── RentalRepository.java
│   └── Rental.java
└── payment
    ├── PaymentController.java
    ├── PaymentService.java
    ├── PaymentRepository.java
    └── Payment.java
```

다만 “요즘의 트렌드”라는 이유만으로 선택하기보다 다음을 기준으로 정해야 한다.

* 변경되는 코드가 함께 배치되는가
* 모듈 간 의존성이 명확한가
* 팀원이 기능 위치를 쉽게 찾을 수 있는가
* 특정 계층에 코드가 과도하게 집중되지 않는가
* 향후 모듈 분리가 가능한가

## 12. Plan Mode

Plan Mode에서는 Claude가 먼저 코드와 관련 파일을 조사하고 변경 계획을 작성한다. 계획이 승인되기 전에는 실제 코드 변경을 제한할 수 있다.

다음 작업에 우선 사용하는 것이 좋다.

* 여러 파일에 걸친 기능 구현
* 기존 구조를 파악해야 하는 작업
* DB 스키마 변경
* API 계약 변경
* 대규모 리팩터링
* 보안 및 인증 구조 수정
* 영향 범위가 불명확한 버그
* 팀원의 코드와 충돌할 가능성이 큰 작업

반면 명확한 오타 수정이나 작은 단일 파일 변경에서는 반드시 사용할 필요가 없다.

Plan Mode에서 확인할 항목은 다음과 같다.

* 어떤 파일을 읽었는가
* 어떤 파일을 수정할 것인가
* 기존 동작이 어떻게 바뀌는가
* 호환성이 깨지는가
* 테스트는 어떻게 추가할 것인가
* 롤백 방법은 무엇인가
* 불확실한 가정이 있는가

## 13. 커스텀 오케스트레이션

Claude Code는 이미 다음 기능을 제공한다.

* 파일 탐색과 수정
* 도구 호출 반복
* Plan Mode
* 서브에이전트
* Agent Teams
* Hooks
* Skills
* MCP
* 세션 관리
* 자동 컨텍스트 압축

따라서 5주 규모의 소규모 프로젝트에서 별도의 멀티에이전트 오케스트레이터나 복잡한 Agent SDK 구조를 먼저 만드는 것은 오버엔지니어링이 될 가능성이 높다.

커스텀 오케스트레이션은 다음과 같은 요구가 실제로 발생했을 때 검토하는 것이 적절하다.

* 정해진 워크플로를 반복 실행해야 함
* 여러 에이전트의 역할과 권한을 엄격히 분리해야 함
* 실행 과정과 비용을 중앙에서 추적해야 함
* CI/CD 또는 사내 시스템에 에이전트를 내장해야 함
* 사람의 승인 단계를 프로그램적으로 제어해야 함
* Claude Code 기본 기능으로 해결되지 않는 반복 병목이 확인됨

먼저 Claude Code의 기본 기능으로 프로젝트를 진행하고, 실제 문제를 측정한 뒤 필요한 부분만 자동화한다.

## 14. 소규모 프로젝트 권장 운영 방식

5주 규모의 팀 프로젝트라면 다음 정도로 시작하는 것이 적절하다.

```text
프로젝트 루트
├── CLAUDE.md
├── README.md
├── docs/
│   ├── architecture.md
│   └── decisions/
├── .claude/
│   ├── settings.json
│   └── skills/        # 반복 작업이 생긴 경우에만 추가
└── 소스 코드
```

권장 원칙:

1. 루트 `CLAUDE.md` 하나로 시작한다.
2. 작업 영향 범위가 크면 Plan Mode를 사용한다.
3. 독립된 새 작업은 `/clear` 후 시작한다.
4. 긴 작업은 `/compact`로 핵심 상태를 보존한다.
5. 나중에 이어갈 작업은 `/rename`과 `/resume`으로 관리한다.
6. 필요하지 않은 MCP 서버는 비활성화한다.
7. 긴 로그와 검색 결과를 무분별하게 컨텍스트에 넣지 않는다.
8. 반복 작업만 Skill로 분리한다.
9. 반드시 지켜야 하는 규칙은 Hook·테스트·CI로 강제한다.
10. 중요한 코드 변경은 Git 브랜치와 커밋으로 관리한다.
11. 복잡한 커스텀 오케스트레이션은 실제 병목이 확인된 뒤 도입한다.

# 2026-07-20 일일회고

## 오늘 학습한 내용

오늘은 운영체제의 가상 메모리와 Java 동시성 개념을 집중적으로 정리했다.

페이지 폴트는 단순한 인터럽트가 아니라, CPU가 명령을 실행하다가 필요한 페이지가 물리 메모리에 없을 때 발생하는 동기적 예외이다. 빈 프레임이 없으면 희생 페이지를 선택하며, dirty page는 디스크에 변경 내용을 기록해야 하므로 clean page보다 교체 비용이 크다. 작업 집합을 메모리에 유지하지 못해 페이지 폴트가 반복되는 현상이 쓰레싱이며, 프로세스를 종료하기보다 일부 프로세스를 일시 중단하거나 스왑 아웃해 완화할 수 있다는 점도 알게 됐다.

TLB는 가상 페이지와 물리 프레임의 변환 결과를 저장하는 하드웨어 캐시다. 같은 프로세스의 스레드는 주소 공간과 페이지 테이블을 공유하므로 스레드 전환 시 일반적으로 TLB를 비울 필요가 없다. 스레드 전환에서는 스택 전체가 아니라 PC, SP, 범용 레지스터 등의 실행 문맥을 저장하고 복원한다.

동시성에서는 `count++`가 읽기·수정·쓰기의 복합 연산이라 Lost Update가 발생할 수 있음을 확인했다. `volatile`은 가시성을 보장하지만 원자성은 보장하지 않는다. `AtomicInteger`는 비교와 교체를 하나의 원자적 명령으로 수행하는 CAS를 사용하며, 충돌이 심하면 반복 재시도로 CPU를 낭비할 수 있다. `synchronized`와 `Lock`은 임계 구역에 하나의 스레드만 진입하도록 상호 배제를 제공한다.

## 프로젝트 적용

퀵서비스 프로젝트의 배차 동시 수락 문제도 결국 공유 상태에 대한 경쟁 조건이다. 단순 조회 후 수정으로 처리하면 여러 라이더가 같은 주문을 수락할 수 있으므로, DB 락·조건부 업데이트·낙관적 락 등을 비교해야 한다. 구현 방식만 선택하기보다 경쟁 강도, 대기 시간, 재시도 비용까지 측정해 결정할 필요가 있다.

## 보완할 점

용어를 대략적인 맥락으로만 말하는 경향이 있었다. 앞으로는 `멀티프로세싱`과 `다중 프로그래밍`, `TLB miss`와 `page fault`, `가시성`과 `원자성`처럼 유사한 개념을 정확히 구분해 설명해야겠다.

## 메인프로젝트

오늘은 퀵서비스 프로젝트의 기능명세를 정리하고, 배송 주문과 라이더의 상태 모델을 확정했다.

먼저 배송 주문 상태를 `WAITING → ASSIGNED → MOVING_TO_PICKUP → PICKED_UP → DELIVERING → COMPLETED`로 정의하고, 배차 실패나 고객 취소는 `WAITING → CANCELED`로 제한했다. 각 상태가 단순한 이름이 아니라 실제 기능과 연결되어야 한다는 점도 확인했다. 예를 들어 배차 확정 시 주문은 `ASSIGNED`, 라이더는 `BUSY`로 함께 변경되어야 하며, 배송 완료 시 주문은 `COMPLETED`, 라이더는 다시 `AVAILABLE`로 전환되어야 한다.

라이더 운행 상태는 `UNAVAILABLE`, `AVAILABLE`, `BUSY`로 확정했다. 회원가입 직후에는 `UNAVAILABLE`, 운행 시작 시 `AVAILABLE`, 배차 확정 시 `BUSY`가 된다. 라이더가 직접 변경할 수 있는 상태와 시스템이 자동으로 변경하는 상태를 구분하면서 상태 관리 책임이 더 명확해졌다.

기능명세에서는 고객·라이더의 휴대전화 인증을 공통 기능으로 정리하고, 로그인·로그아웃·주문 조회·배차·픽업·배송 완료·자동 취소 기능에 사전 조건, 처리 흐름, 성공 조건, 예외 처리를 구체화했다. 특히 주문과 라이더 상태의 동시 변경, 중복 요청에 대한 멱등 처리, 배차와 자동 취소의 경쟁 조건처럼 구현 단계에서 발생할 정합성 문제를 명세에 미리 반영했다.

오늘 작업을 통해 기능명세는 단순한 기능 목록이 아니라 API, DB, 화면, 테스트의 기준이 된다는 점을 배웠다. 앞으로 상태가 변경되면 기능명세, 상태 전이표, DB 제약조건, 코드의 Enum, 실시간 이벤트와 테스트까지 함께 회귀 검토해야 한다.

# 데일리 회고 — 2026-08-06 (be-DailyJob)

작업량 보고가 아니라, 오늘 내가 어떤 판단을 했고 무엇을 반복하거나 바꿔야 하는지 확인하기 위한 기록이다.

## 1. 오늘 완료한 것

### 실제로 끝낸 것

- **계정 설정 화면 로그아웃 연동(#222) 구현 → PR #388 머지**(dev `0bc7b7d`, 12:15).
  껍데기였던 `account/settings.tsx` 를 만들고 가드가 넘겨준 세션 역할로 고객·라이더 로그아웃 훅 중
  하나만 호출하게 했다. 리뷰 중 팀원이 **"500 에러 발생 시 처리가 제대로 되냐"** 고 물어서 코드를
  다시 확인하고 답했다: 실패를 세 갈래로 나눠 다룬다(`resolveLogoutFailure`) —
  - 409(라이더 BUSY)는 **서버가 명시적으로 거부**한 것이라 세션이 확실히 살아 있다 → 캐시를 비우지
    않고 화면에 머문다
  - 401 은 이미 세션이 없다는 뜻이라 성공 경로로 흡수한다
  - **네트워크 오류·5xx 는 성사 여부를 모르는 상태** → 이 기기의 쿼리 캐시와 역할 힌트만 비우고
    자동 이동 대신 안내 + 버튼(다시 시도 / 처음 화면으로)으로 다음 행동을 유도한다
- **라이더 위치 마커 개선**(커밋 `7d89dc3`, PR #376 → dev). 기본 핀 대신 오토바이 이미지, 배경
  flood-fill 투명화, 실루엣 변화에 맞춘 anchor offset 재계산(49→48), **경도 delta 기반 좌우 반전 +
  11m dead zone**(정지 재전송·GPS 지터가 방향을 뒤집지 않게).
- **OSRM 도입 결정 및 인프라 배포 완료(#407 PoC → #416)**.
  서울특별시 bbox(`126.73,37.40,127.20,37.72`)로 스코프를 좁혀 서빙 메모리 **149MB** 를 측정하고,
  새 인스턴스 없이 **기존 Redis 인스턴스(`10.0.130.0`)에 co-locate** 하기로 결정 → Docker 설치,
  로컬 전처리 산출물(237MB) 전송, `osrm-seoul` 컨테이너 기동(`--algorithm mld`, 메모리 상한 400MiB,
  실사용 162.1MiB), WAS 보안그룹에서만 5000 포트 허용까지 마쳤다.
  라우팅 검증(서울시청→강남역): `distance` 9,945.2m / `duration` 730.7초 — 로컬 PoC 측정치와 일치.
- **팀원 PR(#409/#410, 배송 상태 전이 SSE 발행) 리뷰에서 유실 시나리오를 제기**하고, 그 대응으로
  **백업 폴링을 설계해 이슈로 확정(#422)**. 마침 내가 OSRM ETA·예상경로를 폴링으로 받아오기로 했으니
  같은 요청에 상태까지 실어 재조회하면 된다 — TanStack Query `refetchInterval` 약 1분, 추적 불가
  상태(COMPLETED·CANCELED)에서는 중단.

### 아직 끝내지 못한 것

- **백엔드 OSRM 클라이언트 연동(#420)·추적 API 의 ETA·경로 응답(#421)·프론트 경로선 렌더링과 백업
  폴링(#422)** 은 이슈로 쪼개 두기만 하고 코드는 시작하지 않았다. 오늘은 "OSRM 을 부를 수 있는
  상태"까지가 끝이다.
- **인스턴스 재부팅 후 컨테이너 자동 기동 검증**(`--restart unless-stopped` 를 걸었으나 재부팅을
  해보지 않음).
- 로그아웃 화면의 **실제 브라우저 동작 확인**(성공 후 `queryClient.clear()` → 랜딩 이동 순서에서
  화면이 깜빡이지 않는지). 단위 테스트 6건만으로 머지했다.

## 2. 오늘 가장 의미있었던 기술적 판단

**내가 내린 판단**: ETA·예상경로용 라우팅 엔진으로 유료 API 대신 **OSRM 을 직접 띄우되, 새 인스턴스를
만들지 않고 기존 Redis 인스턴스에 co-locate 하고, 지도 데이터는 서울특별시로 한정하고, 전처리는 배포
인스턴스가 아니라 내 로컬에서 한 번만 돌린다.**

**그렇게 판단한 이유**

- 비용이 0이다. 그리고 t3.micro 한 대의 메모리가 **놀고 있다는 것을 추정이 아니라 측정으로 확인**했다.
  Redis 실측은 `used_memory` 894.92KB / RSS 10.08MB / `dbsize` 0 이고, 인스턴스 available 은 532Mi 였다.
  스코프별 서빙 메모리는 전국 1.15GB → 수도권 320MB → **서울 149MB** 라, 서울로 좁히는 것만으로
  1GiB 인스턴스에 들어간다.
- 전처리(`extract`→`partition`→`customize`)는 피크 메모리 522MB·CPU 스파이크가 커서 t3.micro 의 버스트
  크레딧을 태운다. **부하가 큰 단계와 상시 구동 단계를 분리**하면, 배포 인스턴스는 `osrm-routed` 만
  들고 있으면 된다. 전처리 결과(`.osrm*`)는 지도 데이터가 바뀔 때만 갱신하면 되는 정적 산출물이다.

**고려했지만 선택하지 않은 대안**

- **카카오/Tmap 길찾기 API**: 서비스 자체는 검증돼 있지만 호출량 과금이 붙고, 폴링 주기(=재계산 주기)를
  비용이 결정하게 된다. MVP 규모에서 굳이 종속을 만들 이유가 없었다.
- **OSRM 전용 EC2 신규 인스턴스**: 자원 격리는 확실하지만 인스턴스가 하나 늘고 비용도 늘어난다.
  메모리 실측 결과 co-locate 로 충분해서 탈락.
- **전국·수도권 스코프**: 전국은 서빙 1.15GB 로 1GiB 인스턴스에 애초에 안 들어가고, 수도권(320MB)은
  들어가긴 하지만 여유가 반으로 준다. 서비스 지역이 서울로 확정된 이상 살 필요가 없는 여유였다.
- **배포 인스턴스에서 직접 전처리**: 파일 전송이 필요 없어 간단하지만, 위 CPU·메모리 스파이크를
  운영 중인 Redis 와 같은 인스턴스에서 일으키게 된다.

**이 판단이 코드 품질/테스트/유지보수에 미친 영향**

- OSRM 컨테이너에 `--memory=400MiB` 상한을 걸어 **Redis 와 자원 경합·OOM 을 격리**했다. co-locate 의
  가장 큰 리스크를 배포 옵션 한 줄로 막은 셈이다.
- 대신 유지보수 부담이 생겼다: ① **서울 밖 좌표는 라우팅이 실패**하므로 백엔드 클라이언트가 실패를
  정상 흐름으로 다뤄야 한다(ETA 없이도 화면이 동작해야 한다), ② 지도 데이터 갱신 시 **로컬 전처리 →
  파일 전송** 절차가 사람 손을 탄다 — 문서화가 후속으로 남았다.
- 백엔드 연동은 **인터페이스로 분리**해 카카오/Tmap 전환 여지를 남기기로 했다(#420). 정확도·응답속도를
  상용 API 와 비교하지 못한 상태라, 이 비교가 나중에 뒤집힐 수 있다는 전제를 코드 구조에 담아 둔다.

## 3. 가장 막혔던 문제와 해결 과정

**문제 상황**: Redis 인스턴스에 Docker 를 설치하려는데 `sudo dnf install -y docker` 가 실패했다.
패키지 문제가 아니라 **아웃바운드 인터넷이 전부 막힌** 상태였다.

**처음 추측한 원인**: 보안그룹 아웃바운드 규칙이나 DNS. 사설 서브넷 인스턴스라 SSH 도 원래 막혀 있어서
(작업을 위해 WAS→Redis 22번을 임시 개방), "네트워크가 애초에 잠겨 있는 인스턴스"라는 인상 때문에
보안그룹부터 의심했다.

**원인을 확인하기 위해 시도한 방법**

1. 보안그룹 아웃바운드 확인 → 문제 없음
2. 라우팅 테이블 확인 → `Turkey-rtb-private1-ap-northeast-2a` 의 `0.0.0.0/0` 이 `eni-044b7b2da84014fd8`
   를 가리키는데 상태가 **블랙홀**. NAT 인스턴스를 껐다 켜면서 ENI 참조가 깨진 것으로 보였다
3. 콘솔에서 대상을 재지정해 라우트를 활성으로 복구 → **여전히 안 됨**
4. NAT 인스턴스(`10.0.3.252`)에 직접 들어가 `net.ipv4.ip_forward` 확인 → `1`(정상).
   `iptables -t nat -L POSTROUTING` 확인 → **비어 있음**

**실제 원인**: 두 단계였다. ① 라우팅 테이블의 블랙홀, ② NAT 인스턴스 재부팅으로 **iptables
MASQUERADE 규칙이 유실**된 것. ①만 고치고 끝냈으면 여전히 안 됐을 문제라, 중간에 "고쳤는데 안 되네"
에서 멈추지 않고 한 단계 더 들어간 게 갈림길이었다.

**해결 방법**

```bash
sudo iptables -t nat -A POSTROUTING -o ens5 -s 10.0.0.0/16 -j MASQUERADE
```

여기서 멈추면 다음 재부팅에 똑같이 터지므로 `iptables-persistent` 설치 + `netfilter-persistent save`
로 `/etc/iptables/rules.v4` 에 영구화하고 저장을 확인했다.

**다음에 비슷한 문제가 생기면 먼저 확인할 것**

- 사설 서브넷 인스턴스의 인터넷이 안 되면 **보안그룹보다 라우팅 테이블의 `0.0.0.0/0` 상태(블랙홀
  여부)를 먼저** 본다. 보안그룹은 "열려 있는데 안 됨"을 설명하지 못한다.
- 그 다음은 NAT 인스턴스 안쪽: `ip_forward` → `iptables -t nat -L POSTROUTING` 순서.
- 공통 교훈은 **"재부팅으로 사라지는 상태"를 의심하라**는 것이다. 커널 파라미터·iptables 규칙처럼
  런타임에만 존재하는 설정은 인스턴스를 껐다 켠 직후 조용히 비어 있다.
- 인프라 전제도 확인 없이 넘겨짚지 말 것. 오늘만 해도 사설 IP 가 `10.0.130.0`(보통 예약되는 `.0`),
  OS 가 Amazon Linux 2023(사용자명 `ec2-user`, WAS/DB 와 다름), Redis 서비스 유닛명이 `redis6.service`
  로 전부 예상과 달랐다.

## 4. 오늘 잘한 행동

- **팀원 PR 리뷰에서 "동작하냐"가 아니라 "실패하면 어떻게 되냐"를 물었다.** 배송 상태 전이를 SSE 로
  발행하는 PR(#409/#410)을 보고 *이벤트가 유실되면 화면은 영영 갱신되지 않는 것 아니냐* 고 지적했다.
  근거를 세 가지로 정리해 이슈(#422)에 남겼다 — ① 연결이 끊긴 동안 발행된 이벤트는 재연결해도 오지
  않는다(서버가 미전송 이벤트를 쌓지 않음), ② heartbeat 가 없어 **끊긴 연결에 대한 첫 쓰기는 소켓
  버퍼에 들어가 성공한 것처럼 보인다**(CLAUDE.md 실측), ③ Redis Pub/Sub 팬아웃은 fire-and-forget 이라
  그 순간 구독자가 없으면 메시지는 버려진다.
- **그 대응을 새 메커니즘이 아니라 이미 있는 경로의 재사용으로 설계했다.** #399 가 "status 이벤트 →
  `trackingQuery.refetch()`" 구조를 만들었으니, 백업 폴링은 **같은 refetch 를 주기적으로 한 번 더
  부르는 것**뿐이다. 게다가 OSRM ETA·경로를 어차피 폴링으로 받아오기로 했으므로 그 요청에 상태를
  얹으면 호출도 늘지 않는다. 화면 갱신 로직이 두 벌이 되지 않는다.
- **결정을 추정이 아니라 측정 위에 올렸다.** Redis co-locate 가능 여부를 감으로 판단하지 않고, 로컬에
  같은 Redis 7.4 를 띄워 **실제 코드의 키 스키마**(`RedisSessionStore` 세션 HSET, `RiderLocationRepository`
  위치 SET+EX)를 그대로 재현해 세션 1,000개 + BUSY 라이더 300개를 채워 측정했다(키당 ~300바이트).
  그리고 배포 전 실제 인스턴스에서 다시 확인해 로컬 예측치와 일치하는 것을 봤다.
- **되돌려야 할 것을 결정 시점에 이슈 체크박스로 적어 뒀다**(임시 개방한 22번 포트, 5000 포트 소스 제한).
  작업 중 늘어나는 임시 상태를 기억에 맡기지 않았다.

**다음에도 반복하고 싶은 점**: 리뷰에서 정상 흐름 대신 유실·중복·재연결을 묻는 것, 그리고 "충분해
보인다" 대신 숫자를 만들어 놓고 결정하는 것.

## 5. 오늘 아쉬웠던 행동

- **마커 작업을 사실상 전부 Claude 에 맡기고 결과만 확인했다.** 동작은 하지만 `RIDER_MARKER` 의
  offset(32,51)→(32,48) 재계산 근거나 방향 반전의 **dead zone 11m** 같은 상수를 내가 스스로 설명하지
  못한다. 원인은 "화면에 잘 보이면 통과"로 검수 기준을 낮춘 것 — 시각적 결과물이라 눈으로 확인되면
  끝났다고 느꼈다.
  → **다음 행동**: 생성된 코드에 매직넘버가 있으면 머지 전에 그 값이 어디서 왔는지 한 줄로 적을 수
  있는지 확인하고, 못 적으면 물어보거나 주석·테스트로 근거를 고정한다.
- **재부팅 검증을 미룬 채 "인프라 작업 종료"라고 이슈에 적었다.** `--restart unless-stopped` 는 걸었지만
  실제로 껐다 켜보지 않았다. 오늘 겪은 사고가 **정확히 재부팅으로 상태가 날아간 것**(NAT iptables)인데,
  같은 종류의 리스크를 확인 비용이 재부팅 1회밖에 안 되는 상황에서 남겨 뒀다.
  → **다음 행동**: 상시 구동 컨테이너를 띄우면 그 자리에서 `reboot` 후 기동 확인까지 하나의 작업으로
  묶는다. "재발 방지를 iptables 에는 적용하고 컨테이너에는 적용 안 함"은 앞뒤가 안 맞는다.
- **로그아웃 화면을 실제 브라우저로 확인하지 않고 머지했다.** 단위 테스트 6건은 실패 분류 순수 함수만
  검증하고, 성공 후 `queryClient.clear()` → 랜딩 이동 순서 같은 실제 화면 거동은 잡지 못한다.
  마침 팀원이 500 에러 처리를 물어봤을 때 답한 근거도 전부 코드 독해였지 실행 결과가 아니었다.
  → **다음 행동**: 화면 이슈는 "테스트 통과"와 별개로 **핵심 흐름 1회 수동 실행**을 완료 조건에 넣는다.
  최소한 성공 경로와 오늘 답변의 근거였던 실패 경로 하나는 직접 돌려 본다.
- **작업 순서상 보안그룹을 열고 → 닫기로 적고 → 결국 열어 두기로 뒤집었다**(22번, 소스 `10.0.0.0/16`
  이라 VPC 내부 전용이라는 근거로). 결론 자체는 문제 없지만, **개방 시점에 "이건 임시인가 상시인가"를
  판단했으면 왕복이 없었다.**
  → **다음 행동**: 접근 경로를 열 때 소스 범위를 먼저 보고 임시/상시를 그 자리에서 정한다.

# 2026년 7월 13일 일일 회고

## 오늘 학습한 내용

오늘은 크게 다음 세 가지 주제를 학습했다.

1. 스프링 프레임워크와 직접 구현한 웹 서버의 구조 비교
2. JDBC와 드라이버가 담당하는 역할
3. 데이터베이스의 저장 구조와 B-Tree 인덱스

---

# 1. 스프링 프레임워크와 직접 구현한 웹 서버의 구조

오늘 가장 중요하게 이해한 부분은 **서버 기동 과정과 요청 처리 과정은 서로 다른 흐름**이라는 점이다.

서버가 시작될 때 한 번만 실행되는 흐름과, 클라이언트 요청이 들어올 때마다 반복되는 흐름을 구분해서 볼 수 있게 되었다.

## 서버 기동 과정

서버 기동 시에는 애플리케이션이 WAS의 구성요소를 조립한다.

```text
Main.main()
→ RouteRegistry에 URL과 Controller 등록
→ Dispatcher 생성
→ WebServer에 Dispatcher 주입
→ ServerSocket 생성
→ accept() 대기
```

`AppConfig`에서 HTTP 메서드와 경로에 대응하는 컨트롤러를 등록하고, 해당 정보를 `Dispatcher`에 주입한다.

```java
registry.register("POST", "/login", new LoginController())
        .register("GET", "/", new HomeController());
```

이후 `WebServer`가 실행되면 `ServerSocket`을 열고 `accept()`에서 클라이언트 연결을 기다린다.

이 과정에서 중요한 점은 **애플리케이션이 WAS의 부품을 조립한 뒤 WAS에 제어권을 넘긴다**는 것이다.

이는 스프링 프레임워크에서도 비슷하게 나타난다. 개발자는 컨트롤러와 비즈니스 로직을 작성하지만, 객체 생성과 요청 분배, 생명주기 관리는 프레임워크가 담당한다.

## 요청 처리 과정

클라이언트 연결이 들어오면 Acceptor 스레드는 연결을 수락하고 `RequestHandler`를 생성한다.

```text
accept()
→ RequestHandler 생성
→ Thread Pool에 작업 제출
→ Worker Thread가 run() 실행
```

`executorService.submit(handler)`는 요청을 즉시 처리하는 것이 아니라, 작업 큐에 넣는 동작이다. 이후 유휴 Worker Thread가 작업을 가져가 실행한다.

Worker Thread는 다음 순서로 요청을 처리한다.

```text
소켓에서 HTTP 바이트 읽기
→ HttpRequest 생성
→ Dispatcher에 전달
→ RouteRegistry에서 Controller 조회
→ Controller.service() 실행
→ HttpResponse 생성
→ 소켓으로 응답 전송
```

`RequestHandler`는 요청을 직접 판단하지 않고 `Dispatcher`에 처리를 위임한다. `Dispatcher`는 HTTP 메서드와 경로를 기준으로 등록된 컨트롤러를 찾는다.

```java
Controller controller = registry.find(method, path);
controller.service(request, response);
```

컨트롤러는 상태 줄, 헤더, CRLF 등을 직접 조립하지 않고 `HttpResponse`에 의도를 전달한다.

```java
response.sendRedirect("/index.html", "SID=...");
response.ok(bodyBytes, "text/html");
```

실제 HTTP 응답 형식을 만들고 바이트를 쓰는 책임은 WAS에 있다.

## WAS와 애플리케이션의 경계

오늘 학습을 통해 다음과 같이 역할을 구분할 수 있게 되었다.

### WAS의 책임

* 소켓 연결 수락
* 스레드풀 관리
* HTTP 요청 파싱
* URL에 맞는 컨트롤러 탐색
* HTTP 응답 생성
* 정적 리소스 제공
* 소켓을 통한 바이트 송수신

### 애플리케이션의 책임

* URL과 컨트롤러 매핑
* 로그인과 회원가입 등의 비즈니스 로직
* 세션 확인
* 데이터베이스 접근
* 응답할 데이터 결정

WAS는 구체적인 애플리케이션을 알지 못하며, `Controller` 인터페이스만 알고 있다. 반대로 애플리케이션은 WAS가 제공하는 여러 구성요소를 사용한다.

따라서 의존성은 다음과 같이 단방향으로 구성된다.

```text
Application → WAS
```

이 구조를 통해 프레임워크가 애플리케이션 코드를 직접 알지 않으면서도, 인터페이스와 객체 주입을 통해 애플리케이션을 실행할 수 있다는 것을 이해했다.

## 객체의 생명주기

객체마다 생성되는 시점도 다르다.

### 서버 기동 시 한 번 생성되는 객체

* `RouteRegistry`
* `Dispatcher`
* 각 `Controller` 인스턴스
* `WebServer`
* `ExecutorService`

이 객체들은 여러 Worker Thread가 공유한다. 따라서 컨트롤러가 상태를 필드에 저장하면 동시성 문제가 발생할 수 있으므로, 가능한 한 **상태를 갖지 않는 stateless 구조**로 작성해야 한다.

### 연결 또는 요청마다 생성되는 객체

* `RequestHandler`: 연결마다 생성
* `HttpRequest`: 요청마다 생성
* `HttpResponse`: 요청마다 생성

오늘 학습을 통해 스프링의 컨트롤러가 기본적으로 싱글턴 객체로 관리되는 이유와, 컨트롤러 필드에 요청별 상태를 저장하면 안 되는 이유도 더 명확하게 이해할 수 있었다.

---

# 2. JDBC와 데이터베이스 드라이버

두 번째로 JDBC의 주요 객체와 데이터베이스 드라이버의 역할을 학습했다.

스프링이나 JPA를 사용할 때는 데이터베이스 연결 과정이 추상화되어 있었기 때문에 내부 흐름을 크게 의식하지 않았다. 그러나 JDBC 드라이버를 직접 구현해보면서 각각의 객체가 담당하는 역할을 확인할 수 있었다.

## JDBC 실행 흐름

```text
애플리케이션
→ DriverManager
→ Driver
→ Connection
→ Statement
→ SQL 실행
→ ResultSet
```

### Driver

`Driver`는 JDBC 인터페이스와 특정 데이터베이스를 연결하는 구현체다.

애플리케이션은 JDBC라는 공통 인터페이스를 사용하지만, 실제 통신 프로토콜이나 저장 방식은 MySQL, PostgreSQL 등의 드라이버가 처리한다.

### Connection

`Connection`은 데이터베이스와의 연결 및 세션을 표현한다.

주요 책임은 다음과 같다.

* 데이터베이스 연결 유지
* 트랜잭션 관리
* `Statement` 생성
* `commit`, `rollback`
* 연결 종료

Connection은 단순한 자바 객체가 아니라 실제 DBMS와 연결된 네트워크 소켓, 세션 등의 자원을 포함할 수 있기 때문에 생성 비용이 크다.

따라서 실무에서는 요청마다 새로 만들기보다 커넥션 풀에서 빌려 사용하고 반환한다.

### Statement

`Statement`는 SQL 실행을 담당한다.

```java
Statement statement = connection.createStatement();
ResultSet resultSet = statement.executeQuery(sql);
```

Statement는 전달받은 SQL을 파싱하거나 DBMS가 이해할 수 있는 형식으로 전달하고, 실행 결과를 받아온다.

실제 상용 JDBC 드라이버에서는 SQL 파싱과 실행 대부분을 DBMS가 담당하지만, 이번 과제에서는 직접 SQL을 분석하고 실행하는 구조를 구현하면서 JDBC와 DBMS가 제공하는 추상화를 간접적으로 체험했다.

### ResultSet

`ResultSet`은 조회 결과를 행 단위로 탐색할 수 있도록 만든 객체다.

```java
while (resultSet.next()) {
    String name = resultSet.getString("name");
}
```

애플리케이션은 데이터가 파일의 어느 위치에 저장되어 있는지 알 필요 없이 `next()`, `getString()`, `getInt()`와 같은 메서드만 사용하면 된다.

## JDBC와 VFS의 유사성

JDBC의 구조를 보면서 운영체제의 VFS와 유사하다고 느꼈다.

VFS는 ext4, XFS, NFS와 같은 서로 다른 파일시스템을 공통 인터페이스로 다룰 수 있도록 한다.

```text
애플리케이션
→ VFS 인터페이스
→ 실제 파일시스템 구현체
```

JDBC도 이와 비슷하다.

```text
애플리케이션
→ JDBC 인터페이스
→ 데이터베이스별 Driver 구현체
```

애플리케이션은 구체적인 구현을 직접 알지 않고 공통 인터페이스를 사용한다. 실제 동작은 드라이버가 책임진다.

이번 과제를 통해 객체지향에서 인터페이스가 단순히 메서드 목록을 선언하는 것이 아니라, **서로 다른 구현체를 동일한 방식으로 사용할 수 있게 만드는 추상화 계층**이라는 점을 확인했다.

---

# 3. Stream, ByteBuffer와 FileChannel

데이터베이스 파일을 읽고 쓰는 과정에서 `Stream`과 `ByteBuffer`, `FileChannel`의 차이도 학습했다.

## Stream

`InputStream`과 `OutputStream`은 데이터를 순차적으로 읽고 쓰는 데 적합하다.

```text
앞에서부터 순서대로 읽기
→ 다음 바이트 읽기
→ 계속 진행
```

HTTP 요청이나 네트워크 소켓처럼 연속적으로 들어오는 데이터를 처리할 때 자연스럽다.

## ByteBuffer와 FileChannel

페이지 기반 데이터베이스는 파일의 특정 위치에 있는 페이지를 직접 읽고 써야 한다.

```text
파일의 0번 페이지 읽기
파일의 10번 페이지 읽기
파일의 3번 페이지 수정
```

이처럼 임의 위치 접근이 필요한 경우에는 `FileChannel`과 `ByteBuffer`가 더 적합하다.

```java
channel.position(pageId * PAGE_SIZE);
channel.read(buffer);
```

`ByteBuffer`는 바이트 배열에 단순히 데이터를 저장하는 것뿐 아니라, 현재 읽기·쓰기 위치를 나타내는 `position`, 읽을 수 있는 범위를 나타내는 `limit`, 전체 크기를 나타내는 `capacity`를 관리한다.

따라서 정수, 문자열 길이, 레코드 메타데이터 등을 바이너리 형식으로 직렬화하거나 역직렬화하기에 적합하다.

이번 학습을 통해 다음과 같이 구분할 수 있게 되었다.

* 네트워크처럼 순차적인 데이터 처리: Stream
* 페이지 단위 파일 접근과 임의 위치 읽기·쓰기: FileChannel + ByteBuffer

---

# 4. 데이터베이스의 레코드와 페이지 저장 구조

데이터베이스는 데이터를 단순히 객체 형태로 저장하지 않고, 파일 내부의 페이지와 레코드 형식으로 저장한다.

## 고정 길이 레코드와 가변 길이 레코드

모든 행의 크기를 동일하게 고정하는 방식은 위치 계산이 간단하지만, 문자열처럼 크기가 달라지는 데이터를 저장할 때 공간 낭비가 크다.

따라서 실제 데이터베이스에서는 가변 길이 레코드를 많이 사용한다.

가변 길이 레코드는 대략 다음과 같은 형태로 저장할 수 있다.

```text
[컬럼 메타데이터][실제 데이터]
```

메타데이터에는 각 컬럼의 시작 위치와 길이가 포함된다.

```text
컬럼 1: 시작 위치 0, 길이 4
컬럼 2: 시작 위치 4, 길이 8
컬럼 3: 시작 위치 12, 길이 15
```

이 정보를 이용해 원하는 컬럼의 위치를 찾을 수 있다.

## Slotted Page

하나의 페이지 안에서는 보통 슬롯 디렉터리 구조를 사용한다.

```text
[페이지 헤더]
[슬롯 디렉터리 → 레코드 위치]
[빈 공간]
[레코드 데이터]
```

슬롯에는 레코드의 실제 오프셋이 저장된다.

```text
레코드 ID
→ 슬롯 번호
→ 실제 레코드 위치
```

레코드가 페이지 안에서 이동하더라도 슬롯이 가리키는 오프셋만 변경하면 된다. 상위 구조에서는 슬롯 번호를 계속 사용할 수 있기 때문에 레코드 위치 변경의 영향을 줄일 수 있다.

즉, 레코드 위치를 한 번 더 간접 참조하는 대신 데이터 이동과 페이지 정리를 유연하게 처리할 수 있다.

## Free Space Map

DBMS는 새로운 레코드를 삽입할 때마다 모든 페이지를 확인할 수 없다.

따라서 각 페이지에 남은 빈 공간을 별도로 관리하는 `Free Space Map`과 같은 구조를 사용한다.

이를 통해 레코드가 들어갈 수 있는 페이지를 빠르게 찾을 수 있다.

---

# 5. Heap Table과 페이지 접근

Heap 방식의 테이블은 레코드를 특정 키 순서로 정렬하지 않고, 공간이 있는 페이지에 저장한다.

이 경우 조건에 맞는 행들이 여러 페이지에 흩어져 있을 수 있다.

인덱스가 없다면 전체 페이지를 순차적으로 읽는 Full Scan이 필요하다. 인덱스가 있더라도 결과 행이 너무 많거나 여러 페이지에 흩어져 있다면 랜덤 페이지 접근이 증가할 수 있다.

예를 들어 인덱스로 10만 개의 행을 찾더라도 각 행이 서로 다른 페이지에 있다면 페이지 접근 비용이 커질 수 있다.

따라서 옵티마이저는 다음 요소를 고려해 실행 계획을 선택한다.

* 조건에 맞는 예상 행의 수
* 전체 테이블 크기
* 인덱스 선택도
* 순차 I/O와 랜덤 I/O 비용
* 캐시된 페이지 여부

인덱스가 있다고 항상 인덱스 스캔을 사용하는 것은 아니다. 결과가 많으면 전체 테이블을 순차적으로 읽는 것이 더 유리할 수 있다.

---

# 6. 파티셔닝과 샤딩

## 파티셔닝

파티셔닝은 하나의 논리적 테이블을 여러 물리적 구획으로 나누는 방식이다.

예를 들어 날짜를 기준으로 주문 테이블을 나눌 수 있다.

```text
orders_2024
orders_2025
orders_2026
```

특정 기간을 조회할 때 조건에 맞지 않는 파티션을 제외하여 읽는 데이터 양을 줄일 수 있다. 이를 파티션 프루닝이라고 한다.

파티셔닝 기준은 자주 사용하는 조회 조건과 데이터 분포를 고려해 선택해야 한다.

## 샤딩

샤딩은 데이터를 여러 데이터베이스 서버에 분산하는 방식이다.

```text
user_id % 4
→ 0번 DB
→ 1번 DB
→ 2번 DB
→ 3번 DB
```

파티셔닝은 하나의 DBMS 내부에서도 구성할 수 있지만, 샤딩은 일반적으로 서버 자체를 여러 대로 나눈다는 차이가 있다.

`user_id % 4` 방식은 RAID 0과 일부 형태가 비슷해 보일 수 있지만 동일한 개념은 아니다.

RAID 0은 블록 단위로 데이터를 여러 디스크에 분산하여 I/O 성능을 높이는 저장장치 기술이고, 샤딩은 행이나 키를 기준으로 데이터를 여러 데이터베이스 노드에 분산하는 애플리케이션·DB 아키텍처다.

---

# 7. 시스템 카탈로그

DBMS는 테이블 데이터뿐만 아니라 테이블의 구조에 대한 정보도 관리한다.

시스템 카탈로그에는 다음과 같은 메타데이터가 저장된다.

* 테이블 이름
* 컬럼 이름과 타입
* Primary Key
* 인덱스 정보
* 제약 조건
* 사용자와 권한
* 통계 정보

`CREATE TABLE`, `ALTER TABLE`, `CREATE INDEX`와 같은 DDL이 실행되면 시스템 카탈로그도 변경된다.

이를 통해 DBMS는 SQL을 파싱했을 때 해당 테이블과 컬럼이 존재하는지, 어떤 인덱스를 사용할 수 있는지 판단할 수 있다.

---

# 8. 대규모 요청과 데이터베이스 커넥션

사용자가 많아지면 일반적으로 다음과 같은 구조로 요청이 처리된다.

```text
사용자
→ 로드밸런서
→ 여러 대의 WAS
→ 각 WAS의 Thread Pool
→ Connection Pool
→ DBMS
```

예를 들어 WAS가 20대이고 각 서버의 커넥션 풀이 30개라면, 이론적으로 DBMS에는 최대 600개의 연결이 생성될 수 있다.

```text
20 × 30 = 600
```

하지만 DB 커넥션은 생성과 유지 비용이 큰 자원이다.

커넥션마다 DBMS 내부에서 세션 상태, 메모리, 트랜잭션 상태, 네트워크 소켓 등을 관리해야 한다. 따라서 애플리케이션 서버의 수와 스레드 수만 늘린다고 전체 처리량이 계속 증가하지는 않는다.

DBMS가 감당할 수 있는 동시 연결 수와 처리량을 기준으로 커넥션 풀 크기를 제한해야 한다.

## Backpressure

애플리케이션이 DBMS의 처리 능력보다 더 많은 요청을 계속 보내면 요청이 누적되고 전체 시스템의 지연시간이 급격히 증가한다.

이를 방지하기 위해 다음과 같은 방식으로 부하를 제어할 수 있다.

* 커넥션 풀 크기 제한
* 요청 큐 크기 제한
* 타임아웃 설정
* Rate Limiting
* 과부하 시 빠른 실패 처리
* 트랜잭션을 짧게 유지
* 무거운 작업을 비동기 처리

Backpressure는 단순히 요청을 빠르게 처리하는 기술이라기보다, 하위 시스템이 감당할 수 있는 양만 전달하도록 압력을 제어하는 개념이다.

---

# 9. Redis 캐시와 쿼리 최적화

DB 접근 횟수를 줄이기 위해 Redis와 같은 캐시를 사용할 수 있다.

```text
요청
→ Redis 조회
→ 캐시가 있으면 즉시 반환
→ 없으면 DB 조회 후 캐시에 저장
```

캐시는 조회 속도를 높이고 DB 부하를 줄일 수 있지만 다음 문제가 추가된다.

* 데이터 만료 시점 관리
* DB와 캐시의 데이터 불일치
* 캐시 무효화
* 캐시 장애
* 특정 키에 요청이 몰리는 Hot Key 문제
* 존재하지 않는 데이터를 반복 조회하는 Cache Penetration

따라서 캐시는 무조건 사용하는 것이 아니라, 읽기 빈도와 데이터 변경 빈도, 정합성 요구사항을 고려해 적용해야 한다.

또한 캐시를 도입하기 전에 불필요한 페이지 접근, 잘못된 인덱스, 비효율적인 조인 등을 개선하는 쿼리 최적화가 먼저 필요할 수 있다.

---

# 10. B-Tree 인덱스와 페이지 접근

데이터베이스는 디스크 I/O 비용을 줄이기 위해 페이지 단위로 데이터를 읽는다.

B-Tree 인덱스의 각 노드도 일반적으로 하나의 페이지에 대응하도록 설계된다.

```text
Root Page
→ Internal Page
→ Leaf Page
```

트리의 높이가 낮을수록 조회할 때 접근하는 페이지 수가 줄어든다. B-Tree는 하나의 노드에 많은 키를 저장하기 때문에 데이터가 많아도 트리 높이가 비교적 낮게 유지된다.

## 클러스터드 인덱스

클러스터드 인덱스 구조에서는 리프 페이지에 실제 행 데이터가 인덱스 키 순서로 저장된다.

```text
B-Tree 탐색
→ Leaf Page
→ 실제 Row
```

따라서 특정 키 범위를 조회할 때 인접한 행들이 가까운 페이지에 저장되어 있어 순차 페이지 접근에 유리하다.

다만 테이블의 물리적 정렬 기준은 일반적으로 하나만 가질 수 있기 때문에 클러스터드 인덱스도 보통 하나만 구성할 수 있다.

## 비클러스터드 인덱스

비클러스터드 인덱스의 리프에는 실제 행 전체가 아니라 행의 위치나 Primary Key가 저장된다.

```text
인덱스 탐색
→ 리프에서 행 위치 또는 PK 확인
→ 테이블 페이지 재접근
```

따라서 조건에 맞는 행이 많으면 테이블 페이지를 여러 번 랜덤 접근할 수 있다.

## 희소 인덱스

희소 인덱스는 모든 레코드마다 인덱스 엔트리를 저장하지 않고, 페이지나 데이터 구간마다 대표 키만 저장하는 방식이다.

데이터가 인덱스 키 순서로 정렬되어 있어야 대표 키를 찾은 뒤 해당 구간을 순차 탐색할 수 있다. 따라서 희소 인덱스는 클러스터링된 저장 구조와 함께 사용할 때 유리하다.

다만 실제 DBMS의 B+Tree 인덱스를 모두 단순한 희소 인덱스라고 볼 수는 없다. DBMS와 인덱스 종류에 따라 리프 레벨의 저장 방식이 다르다.

## 해시 인덱스

해시 인덱스는 평균적으로 동일 조건 검색에 매우 빠르지만, 항상 상수 시간이 보장되는 것은 아니다.

해시 충돌이 많아지거나 버킷 분포가 불균형하면 조회 비용이 증가할 수 있다. 이론적인 최악의 경우에는 하나의 버킷에 데이터가 몰려 선형 탐색에 가까워질 수 있다.

또한 키의 정렬 정보를 보존하지 않기 때문에 다음과 같은 범위 검색에는 적합하지 않다.

```sql
WHERE age >= 20 AND age < 30
```

범위 조건과 정렬이 필요한 일반적인 데이터베이스 인덱스에서는 B-Tree 계열이 널리 사용된다.

---

# 오늘의 핵심 정리

오늘은 단순히 JDBC 객체나 B-Tree의 정의를 암기한 것이 아니라, 웹 서버에서 데이터베이스 파일까지 요청이 흘러가는 전체 계층을 연결해서 이해했다.

```text
클라이언트
→ WAS
→ Controller
→ JDBC
→ Driver
→ DBMS
→ B-Tree
→ Page
→ Record
→ File
```

웹 서버에서는 인터페이스와 Dispatcher를 통해 요청 처리 로직을 분리하고, JDBC에서는 공통 인터페이스와 Driver를 통해 데이터베이스 구현을 추상화한다.

DBMS 내부에서는 SQL이라는 논리적 요청을 페이지와 레코드에 대한 물리적 접근으로 변환한다. 인덱스, 옵티마이저, 버퍼 캐시와 같은 기술은 결국 디스크에서 읽어야 하는 페이지 수를 줄이는 방향으로 동작한다.

오늘 학습한 여러 주제는 서로 분리된 개념처럼 보였지만, 공통적으로 다음 두 가지 목적을 가진다는 것을 알게 되었다.

1. 계층 간 책임을 분리하여 복잡성을 감춘다.
2. 비싼 자원인 스레드, 커넥션, 네트워크 I/O, 디스크 페이지 접근을 최소화한다.

직접 WAS와 JDBC 드라이버를 구현해보면서 스프링과 DBMS가 대신 처리해주던 동작을 구체적으로 이해할 수 있었다. 특히 프레임워크나 드라이버는 단순히 편리한 라이브러리가 아니라, 객체 생성과 요청 분배, 프로토콜 변환, 자원 관리 등 많은 책임을 추상화한 계층이라는 점이 인상적이었다.

# 일일 회고: JVM 내부 구조와 실행 흐름 학습

CNS 면접 이후 Java와 Spring Boot를 더 깊게 이해하기 위해 JVM의 내부 구조를 학습했다. 평소에는 Java 코드를 작성하고 Spring Boot 애플리케이션을 실행하는 수준에서만 JVM을 사용했지만, 오늘은 `.java` 파일이 `.class` 파일로 컴파일되고, JVM이 이를 로딩·검증·실행·최적화하는 전체 흐름을 정리했다.

이번 학습의 핵심은 단순히 JVM 메모리 영역을 암기하는 것이 아니라, **컴파일 타임과 런타임이 어떻게 이어지는지**, 그리고 **클래스 로딩, 메모리 할당, GC, JIT 컴파일러가 각각 어떤 역할을 하는지**를 연결해서 이해하는 것이었다.

# 1. JVM 메모리 영역

먼저 JVM Runtime Data Area에 대해 복습했다. JVM 메모리 영역은 크게 Method Area, Heap, JVM Stack, PC Register, Native Method Stack으로 나눌 수 있다.

Method Area는 클래스 메타데이터, 메서드 정보, 런타임 상수 풀 등을 저장하는 영역이다. HotSpot JVM 기준으로 Java 8 이후에는 이 영역이 주로 Metaspace로 구현된다고 이해했다.

Heap은 `new`로 생성된 객체 인스턴스가 저장되는 영역이다. 예를 들어 `Member m = new Member();`라고 하면 `m`이라는 참조 변수는 Stack에 저장되지만, 실제 `Member` 객체는 Heap에 생성된다.

JVM Stack은 스레드마다 하나씩 존재하며, 메서드가 호출될 때마다 Stack Frame이 쌓인다. Stack Frame 안에는 지역 변수, 매개변수, `this` 참조, Operand Stack, 반환 정보 등이 들어간다. 처음에는 JVM Stack이 지역변수 위주의 공간이라고만 생각했지만, 정확히는 **메서드 실행에 필요한 호출 단위의 작업 공간**이라고 보는 것이 맞다.

PC Register는 현재 실행 중인 바이트코드 명령어 위치를 가리키고, Native Method Stack은 Java가 아닌 C/C++ 같은 native 메서드 실행에 사용될 수 있는 스택이다. JNI는 Java 코드가 JVM 밖의 네이티브 라이브러리와 연결되는 인터페이스라는 점도 함께 정리했다.

이번 학습에서 가장 중요했던 구분은 **객체 인스턴스 데이터와 타입 데이터의 차이**였다. 객체 인스턴스 데이터는 객체마다 다른 실제 상태값이고 Heap에 저장된다. 반면 타입 데이터는 클래스 자체에 대한 설명 정보이며 Method Area 또는 Metaspace에 저장된다. 메서드 코드가 객체마다 복사되는 것이 아니라, 클래스 메타데이터 쪽에 공통으로 존재하고, 객체마다 다른 것은 인스턴스 필드 값이라는 점을 정리했다.

# 2. 메모리 오버플로우

메모리 오버플로우와 Full GC의 차이도 학습했다. Full GC는 JVM이 Heap 전체, 특히 Old 영역까지 포함해서 크게 정리하는 GC 작업이다. 반면 Heap 메모리 오버플로우는 보통 `OutOfMemoryError: Java heap space`처럼 GC를 수행해도 더 이상 객체를 할당할 공간을 확보하지 못했을 때 발생하는 에러다.

즉, Full GC는 청소 시도이고, Heap OOM은 청소를 해도 공간이 부족해서 객체 할당에 실패한 상태다.

객체가 더 이상 참조되지 않으면 GC 대상이 되지만, `List`나 `Map` 같은 컬렉션이 계속 참조를 유지하고 있다면 GC 입장에서는 살아있는 객체로 판단한다. 따라서 실제로는 필요 없는 객체라도 참조가 남아 있으면 메모리 누수가 발생할 수 있다.

예를 들어 무한 루프 안에서 계속 큰 객체를 만들고 리스트에 저장하면, GC가 아무리 돌아도 해당 객체들은 여전히 참조 중이기 때문에 제거되지 않는다. 결국 Heap이 가득 차고 OOM이 발생한다.

이번에 정리한 핵심은 다음과 같다.

Full GC는 장애가 아니라 GC 이벤트다. 하지만 Full GC가 자주 발생하고, GC 이후에도 Heap 사용량이 줄어들지 않는다면 메모리 누수나 과도한 객체 보관을 의심해야 한다. Heap OOM은 GC 이후에도 객체 할당 공간을 확보하지 못할 때 발생하는 장애 상태다.

# 3. 가비지 컬렉터

GC는 Heap에 생성된 객체 중 더 이상 참조되지 않는 객체를 자동으로 회수하는 메커니즘이다. Java 개발자는 C/C++처럼 직접 메모리를 해제하지 않지만, 그렇다고 메모리를 신경 쓰지 않아도 되는 것은 아니다. 객체 참조를 불필요하게 오래 유지하면 GC가 회수할 수 없기 때문이다.

GC는 보통 Young 영역과 Old 영역을 나누어 관리한다. 새로 생성된 객체는 Young Generation에 들어가고, 여러 번 GC를 살아남으면 Old Generation으로 이동할 수 있다. Young GC는 Young 영역을 정리하는 비교적 가벼운 GC이고, Full GC는 Old 영역까지 포함하는 무거운 GC라고 이해했다.

또한 G1 GC와 ZGC에 대해서도 학습했다. G1 GC는 Java 9 이후 HotSpot JVM의 기본 GC로 많이 사용되는 범용 서버용 GC다. Heap을 여러 Region으로 나누어 관리하고, 처리량과 지연시간 사이의 균형을 맞추는 것을 목표로 한다.

ZGC는 더 낮은 pause time을 목표로 하는 저지연 GC다. 큰 Heap에서도 GC로 인한 Stop-The-World 시간을 매우 짧게 유지하는 것을 목표로 한다. 다만 일반적인 Spring Boot 백엔드 서버에서는 처음부터 ZGC를 선택하기보다는, 기본적으로 G1을 사용하고 GC 로그와 응답시간 지표를 보면서 필요할 때 검토하는 것이 현실적이라고 정리했다.

# 4. GC 최적화

GC 최적화는 단순히 GC 알고리즘을 바꾸는 문제가 아니라, 애플리케이션의 객체 생성 패턴과 Heap 사용량을 함께 보는 문제다.

가장 기본적인 최적화는 Heap 크기 설정이다. JVM 옵션에서 `-Xms`는 시작 Heap 크기, `-Xmx`는 최대 Heap 크기를 의미한다. 운영 환경에서는 예측 가능성을 위해 둘을 같게 설정하는 경우가 많다.

예를 들어 다음과 같이 설정할 수 있다.

`-Xms512m -Xmx512m -XX:+UseG1GC`

또한 GC 로그를 확인하는 것이 중요하다. JDK 9 이상에서는 다음과 같은 옵션으로 GC 로그를 남길 수 있다.

`-Xlog:gc*:file=gc.log:time,uptime,level,tags`

GC 최적화에서 더 중요한 것은 불필요한 객체 생성을 줄이는 것이다. 예를 들어 DB에서 대량 데이터를 한 번에 `findAll()`로 가져와 메모리에 올리는 것은 위험하다. 대량 데이터는 페이징이나 스트리밍 방식으로 처리해야 한다.

반복문 안에서 문자열을 `+`로 계속 이어 붙이는 것도 불필요한 객체를 많이 만들 수 있으므로 `StringBuilder`를 사용하는 것이 좋다. 로그를 남길 때도 `log.debug("user = " + user)`보다는 `log.debug("user = {}", user)`처럼 placeholder를 사용하는 것이 불필요한 문자열 생성을 줄이는 데 도움이 된다.

또한 하나의 JVM에 큰 Heap을 주는 것보다, 여러 JVM 인스턴스를 띄우고 각각 적당한 Heap을 주는 구성이 GC pause를 통제하기 쉬울 수 있다는 점도 배웠다. Heap이 작으면 각 JVM이 관리해야 하는 객체 집합이 줄어들고, 한 인스턴스가 GC 중이어도 다른 인스턴스가 요청을 처리할 수 있다. 다만 JVM별 Metaspace, Code Cache, Thread Stack 등의 오버헤드가 중복되고, Heap이 너무 작으면 GC가 너무 자주 발생할 수 있으므로 균형이 필요하다.

# 5. 클래스 파일 구조

컴파일 타임에는 `javac`가 `.java` 파일을 JVM이 실행할 수 있는 `.class` 바이트코드 파일로 변환한다.

`.class` 파일은 맨 앞에 `CAFEBABE`라는 magic number로 시작한다. 이는 JVM이 해당 파일이 Java class file 형식인지 확인하기 위한 식별자다. 이후 minor version, major version, constant pool, access flags, this class, super class, interfaces, fields, methods, attributes 등의 정보가 이어진다.

major version은 해당 class 파일이 어떤 Java 버전으로 컴파일되었는지를 나타낸다. 예를 들어 major version 65는 Java 21을 의미한다. 따라서 `Unsupported class file major version 65` 같은 오류는 Java 21로 컴파일된 class 파일을 현재 실행 환경이 제대로 처리하지 못한다는 의미로 이해할 수 있다.

constant pool에는 문자열 리터럴, 클래스 이름, 메서드 이름, 필드 이름, 타입 정보, 메서드 호출 대상 같은 심볼 참조 정보가 들어간다. 처음에는 constant pool에 클래스의 모든 메타데이터가 들어간다고 생각했지만, 더 정확히는 클래스/필드/메서드/문자열 등에 대한 심볼 정보가 들어간다고 이해했다.

메서드의 실제 실행 내용은 Code attribute 안에 JVM 바이트코드 명령어로 들어간다. 바이트코드 명령어를 모두 외울 필요는 없지만, JVM이 지역변수 배열과 Operand Stack을 이용하는 스택 기반 실행 구조라는 정도는 이해할 필요가 있다.

# 6. 클래스 로더

클래스 로더는 런타임에 필요한 `.class` 파일을 읽어 JVM 내부의 클래스 메타데이터로 올리는 역할을 한다.

클래스 로딩 과정은 크게 Loading, Linking, Initialization으로 나뉜다. Linking은 다시 Verification, Preparation, Resolution으로 나뉜다.

Loading 단계에서는 `.class` 파일을 읽어 클래스 메타데이터를 생성한다. HotSpot JVM 기준으로 클래스 메타데이터는 Metaspace에 저장되고, `java.lang.Class` 객체는 Heap에 생성된다.

Verification 단계에서는 바이트코드가 JVM 규칙을 위반하지 않는지 검증한다. 잘못된 타입 접근, 잘못된 jump, stack underflow/overflow 같은 문제가 없는지 확인한다. 즉 JVM은 아무 class 파일이나 무조건 실행하지 않고, 실행 가능한 안전한 바이트코드인지 먼저 검증한다.

Preparation 단계에서는 static 변수에 메모리를 할당하고 기본값을 설정한다. 예를 들어 `static int count = 10;`이 있다면 Preparation 단계에서는 먼저 `count = 0`이 된다. 이후 Initialization 단계에서 명시적 초기화가 실행되어 `count = 10`이 된다.

Resolution 단계에서는 constant pool의 심볼 참조를 실제 참조로 변환한다. 예를 들어 class 파일에는 `java/lang/System`, `out`, `println` 같은 심볼 정보가 들어 있는데, 런타임에는 이를 실제 클래스, 필드, 메서드 참조로 연결해야 한다.

Initialization 단계에서는 static 변수의 명시적 초기화와 static block이 실행된다.

이번 학습에서 헷갈렸던 부분은 클래스 로더가 모든 변수를 static에 할당하는지 여부였다. 결론적으로 클래스 로더가 다루는 것은 주로 static 변수다. 인스턴스 변수는 클래스 로딩 시점에 값이 생기는 것이 아니라, 객체가 `new`로 생성될 때 Heap 안의 객체에 생긴다. 지역 변수는 메서드가 호출될 때 JVM Stack의 Stack Frame 안에 생긴다.

# 7. JIT 컴파일러

Java의 실행 흐름은 단순히 `.java`를 `.class`로 바꾸는 것에서 끝나지 않는다. JVM은 `.class` 바이트코드를 처음에는 인터프리터로 실행하다가, 자주 실행되는 코드가 발견되면 JIT 컴파일러를 통해 네이티브 코드로 변환한다.

HotSpot VM이라는 이름도 여기서 나온다. 실행 중 자주 호출되는 메서드나 반복문 같은 hot spot을 찾아 최적화하는 JVM 구현체라는 의미다.

JIT 컴파일러가 생성한 네이티브 코드는 Code Cache에 저장되고, 이후 같은 코드를 실행할 때는 인터프리터로 매번 해석하지 않고 컴파일된 네이티브 코드를 사용할 수 있다.

JIT 최적화에는 method inlining, escape analysis 같은 기법이 있다. Method inlining은 자주 호출되는 작은 메서드의 호출 비용을 줄이기 위해 호출 위치에 메서드 본문을 펼치는 최적화다. Escape analysis는 객체가 메서드 밖으로 빠져나가지 않는다고 판단되면 Heap 할당을 제거하거나 더 가볍게 처리할 수 있는 최적화다.

일반적인 Spring Boot 웹백엔드 현업에서는 컴파일러 최적화를 직접 다루는 일은 많지 않다. 성능 문제는 보통 DB 쿼리, 인덱스, 캐시, 네트워크 I/O, 스레드 풀, GC에서 먼저 발생한다. 하지만 JVM이 내부적으로 인터프리터와 JIT를 통해 바이트코드를 실행하고 최적화한다는 흐름을 이해하는 것은 Java 애플리케이션의 성능을 이해하는 데 중요하다.

# 오늘의 핵심 정리

오늘 학습을 통해 Java 프로그램이 실행되는 흐름을 더 구체적으로 이해할 수 있었다.

`.java` 파일은 컴파일 타임에 `javac`를 통해 `.class` 바이트코드 파일로 변환된다. `.class` 파일은 `CAFEBABE`로 시작하고, constant pool, 필드 정보, 메서드 정보, 바이트코드 등을 포함한다. 런타임에는 ClassLoader가 이 class 파일을 Loading, Linking, Initialization 과정을 통해 JVM 내부로 올린다.

클래스 메타데이터는 JVM Stack이 아니라 Method Area 또는 HotSpot 기준 Metaspace에 저장된다. 객체 인스턴스는 Heap에 저장되고, 메서드 실행에 필요한 지역변수와 호출 정보는 JVM Stack의 Stack Frame에 저장된다.

GC는 Heap에서 더 이상 참조되지 않는 객체를 회수한다. Full GC는 Heap 전체를 크게 정리하는 작업이고, Heap OOM은 GC를 해도 객체를 할당할 공간이 없을 때 발생하는 에러다. GC 최적화는 GC 알고리즘 선택뿐 아니라 객체 생성 패턴, Heap 크기, GC 로그 분석, 코드 작성 방식과도 연결된다.

마지막으로 JIT 컴파일러는 자주 실행되는 바이트코드를 네이티브 코드로 변환하여 성능을 높인다. 일반적인 백엔드 개발에서는 JIT 내부 구현까지 깊게 알 필요는 없지만, Java가 단순히 바이트코드를 해석만 하는 것이 아니라 런타임에 최적화된다는 점은 중요하다.

결론적으로 오늘 학습한 내용은 JVM을 단순 암기 과목으로 보는 것이 아니라, **컴파일 → 클래스 로딩 → 메모리 적재 → 실행 → GC → JIT 최적화**라는 하나의 실행 파이프라인으로 이해하는 데 의미가 있었다.

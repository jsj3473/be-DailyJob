# be-DailyJob — 2026-08-04

## 1. 오늘 완료한 것

**끝낸 것**
- 고객 → WAS 방향 위치 폴링 API 개발 (SSE와 비교할 부하테스트용 arm).
- 부하테스트 인프라 학습.

**끝내지 못한 것**
- 부하테스트 실제 실행과 SSE arm과의 수치 비교. arm과 인프라만 준비됐고 측정값은 아직 없다.
- "코드레벨 최적화를 끝냈다"고 볼 기준을 정하지 못했다 — 무엇을 어디까지 보면 실험에 들어가도
  되는지가 감으로 남아 있다.

## 2. 오늘 가장 의미있었던 기술적 판단

**내가 내린 판단**
폴링 API를 새로 짜지 않고 기존 코드(인증·인가 경로, 조회 서비스)를 최대한 그대로 재사용했다.
그리고 부하테스트에 들어가기 전에 코드 레벨 최적화를 먼저 끝내기로 했다.

**그렇게 판단한 이유**
- 두 arm이 다른 인증·인가 경로를 타면 부하테스트 수치가 **SSE와 폴링의 차이인지, 두 구현의 차이인지
  구분할 수 없다.** 비교 가능성이 이 API의 존재 이유다.
- 실험 도중에 코드 레벨 병목이 발견되면 그때까지의 측정값을 버리고 다시 돌려야 한다. 부하테스트는
  한 번 도는 비용이 크므로, 되돌릴 수 있는 변수는 실험 전에 고정해 두는 편이 낫다.


**이 판단이 코드 품질/테스트/유지보수에 미친 영향**
- 신규 코드가 얇아졌고, 인가 규칙이 바뀌면 SSE와 폴링이 같이 바뀐다.
- 실험 시행착오가 줄고 결과 정확도가 오를 것으로 **예상**한다 — 아직 측정으로 확인한 것은 아니다.

## 3. 가장 막혔던 문제와 해결 과정

**문제 상황**
라이더 쪽에서 자기 진행 중 주문의 orderId를 어떻게 확보할 것인가. 로컬스토리지 저장 / Redis 인증
세션에 함께 보관 / 필요할 때마다 GET 요청, 세 가지를 두고 결정하지 못했다.

**처음 가정한 것**
"클라이언트나 세션이 orderId를 들고 있어야 한다" — 매번 조회하는 건 낭비라고 생각했다.

**확인하기 위해 한 것**
팀원과 논의하고, 서버에 이미 있는 조회 경로를 확인했다.

**실제로 확인된 것**
orderId는 **캐시하면 안 되는 값**이었다. 배차·완료는 세션 수명(로그인 시점 2시간 고정 TTL, 슬라이딩
갱신 없음)과 무관하게 일어나서 세션에 넣으면 무효화 시점이 어긋난다. 무효화를 한 번 놓치면 그 라이더의
**다음 배송 경로가 이전 고객에게 흘러간다.** 로컬스토리지도 결국 클라이언트가 보낸 배송 id를 서버가
믿는 구조라, 이미 한 번 닫은 종류의 구멍을 다시 여는 셈이다.

**해결 방법**
이미 구현돼 있는 GET 조회를 그대로 쓰기로 했다. 인덱스가 있어 상수 시간에 가져올 수 있을 것으로
예측한다.

**다음에 비슷한 문제가 생기면 먼저 확인할 것**
- "이 값을 캐시해도 되는가"는 조회 비용이 아니라 **무효화 시점**으로 먼저 판단한다. 값이 바뀌는
  사건(배차·완료·취소)과 캐시의 수명(세션 TTL, 브라우저 저장소)이 다르면 캐시하지 않는다.
- "인덱스가 있으니 상수 시간"은 **어느 조회를 쓰느냐에 따라 갈린다.** 유니크 인덱스 단일 행 조회는
  이력이 쌓여도 비용이 안 늘지만, 라이더의 주문 이력을 훑고 상태로 거르는 조회는 운행 기간에 비례해
  느려진다. 주기적으로 호출할 경로면 실행계획을 확인하고 쓴다.

## 4. 오늘 잘한 행동

**어떤 행동이었는지**
3번 문제를 혼자 결정하지 않고 팀원과 논의했다.

**도움이 된 이유**
처음 생각했던 "Redis 인증 세션에 주문 id를 함께 보관한다"가 틀렸다는 걸 구현 전에 확인했다. 코드로
옮긴 뒤였다면 무효화 누락은 **테스트로 잘 드러나지 않는 종류의 버그**라 한참 뒤에 알았을 것이다.

**다음에도 반복하고 싶은 점**
설계 선택지가 둘 이상일 때 혼자 고르지 않고 근거를 꺼내 놓고 이야기하는 것. 특히 "성능상 이게 나을
것 같다"는 직관이 개입할 때.


**다음에는 다르게 해볼 구체적인 행동**
1. 성능 근거를 O(표기)로 쓰지 않고 **"요청당 외부 왕복 n회 × 동시 사용자 N ÷ 폴링 주기"** 형태로 적는다.
2. 부하테스트 시나리오에 폴링 주기를 2개 이상 넣고, RPS·p95와 함께 **직전 응답과 좌표가 동일한 비율**을
   지표로 뽑는다. 폴링 arm의 낭비를 보여주는 건 그 비율이다.
3. 실험 착수 전에 "여기까지 보면 시작한다" 체크리스트를 3~5줄로 먼저 적는다.

# 2026-07-29 오늘의 회고

## 1. 오늘의 학습 회고

### 1) Redis 학습
- 휴대폰 인증번호 흐름: 인증번호 발급 시 Redis에 등록 → 회원가입 시점에 조회(읽기)와 동시에 원자적으로 토큰 제거. 재사용 공격 방지를 위한 read-and-delete 패턴 적용
- Redis 내부 구조: 해시맵 기반 자료구조 학습
- Master-Slave 복제: 가용성 향상 + 읽기 부하 분산 목적으로 활용되는 구조 이해

### 2) 로그인 기능 - 쿠키 정책 개선
- SameSite 옵션(Strict/Lax/None) 선택 고민
- CloudFront 내에서 정적 파일 경로와 API 경로를 분리하면 same-site 컨텍스트가 성립되어 Lax로도 문제없이 동작한다는 걸 확인
- 경로 구조도 깔끔해지는 부수 효과 → 인프라 구조 개선까지 이어짐

### 3) SSE Emitter 구조 학습 (착수 전 개념 정리)
- Emitter가 직접 FD(파일 디스크립터)를 소유하는 게 아니라, Tomcat이 커넥션(FD)의 실제 소유자
- 라이더 위치 변경 → WAS로 요청 전달 → 작업 스레드가 해당 커넥션에 매핑된 적절한 Emitter를 찾아 이벤트 전달
- 이 구조 이해가 향후 실시간 위치 브로드캐스팅 설계의 기반이 될 것

### 4) 인증/인가 - Filter vs Interceptor
- 세션 검증 로직 위치를 Filter가 아닌 Interceptor에서 처리하기로 결정
- 이유: Filter는 서블릿 스펙 영역이라 MVC(DispatcherServlet) 컨텍스트 밖에 위치 → HandlerMethod, `@Auth` 같은 어노테이션 기반 처리나 컨트롤러 메타데이터 접근이 어려움
- Interceptor는 MVC 흐름 안에서 동작하므로 이런 제약이 없음

---

## 2. 오늘 가장 의미있었던 기술적 판단

**내가 내린 판단:**
프론트(S3)와 API(EC2)를 CloudFront 배포 하나에 묶어서(`/api/*` 경로 behavior 추가) 브라우저 기준 동일 origin으로 만들기로 했다. 쿠키는 `SameSite=None` 대신 `SameSite=Lax` + `Secure`로 유지.

**그렇게 판단한 이유:**
쿠키 옵션(SameSite/Secure)을 확정하려면 먼저 프론트-API 배포 구조가 정해져야 했다. CloudFront에 정적 파일과 API 경로를 함께 두면 브라우저 입장에서 크로스사이트가 아니게 되므로 쿠키가 별도 설정 없이 그대로 전달된다. 또한 origin 접근을 CloudFront로만 제한할 수 있어 구조가 깔끔해진다는 장점도 있었다. `SameSite=None`으로 갈 수도 있었지만, 그러면 EC2에 별도 HTTPS 인증서가 필요해지고 커스텀 도메인 없이는 비용·구성이 늘어나는데, 굳이 그럴 이유가 없다고 판단했다.

**고려했지만 선택하지 않은 대안:**
- API용 CloudFront 배포를 별도로 두거나 EC2를 그대로 노출 → 크로스사이트가 되어 `SameSite=None`+`Secure` 필요, EC2 자체 HTTPS 인증서 요구됨.
- 커스텀 도메인 구매 후 서브도메인 분리(`app.xxx.com`/`api.xxx.com`) → 같은 site 취급되어 `SameSite=Lax`로 충분하지만, 도메인 구매·Route53·ACM 인증서 발급 등 인프라 작업이 추가로 필요함.

**이 판단이 코드 품질/테스트/유지보수에 미친 영향:**
`SessionCookie.java`에서 쿠키 속성을 프로파일별로 단순하게 관리할 수 있게 됐다(local=false, 그 외 기본값=true). 다만 이 결정은 CloudFront에 `/api/*` behavior를 실제로 추가하는 인프라 작업이 완료된다는 전제에 의존하는데, 이건 아직 AWS 콘솔 설정 단계라 코드로 반영되지 않은 미결 상태다. 나중에 API가 별도 배포로 바뀌면 쿠키 설정을 다시 `SameSite=None`으로 되돌려야 하므로, 이 의존성을 CLAUDE.md 등에 명시해두는 게 좋을 것 같다.

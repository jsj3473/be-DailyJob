# 0708 회고

## 오늘 학습한 내용

오늘은 Java의 기본 타입과 참조 타입, 객체지향의 기본 개념, 멀티스레드와 동기화, I/O 스트림, 그리고 웹 서버 5단계에서 구현한 쿠키/세션 기반 로그인 구조를 학습했다.

---

## 1. 기본 타입과 참조 타입

Java의 변수는 크게 기본 타입과 참조 타입으로 나뉜다.

기본 타입은 `int`, `long`, `boolean`, `double`처럼 실제 값을 저장하는 타입이다. 지역 변수로 선언되면 스택 프레임 안에 값 자체가 저장된다.

반면 참조 타입은 객체 자체를 변수에 저장하는 것이 아니라, 힙에 생성된 객체를 가리키는 참조값을 저장한다. 예를 들어 `User user = new User()`라고 하면 `User` 객체는 힙에 생성되고, 변수 `user`에는 그 객체를 가리키는 참조값이 저장된다.

`final` 키워드도 기본 타입과 참조 타입에서 의미가 다르다는 점을 정리했다.

* `final + 기본 타입`은 변수에 저장된 값 자체를 다시 바꿀 수 없다는 의미이다.
* `final + 참조 타입`은 참조 변수가 다른 객체를 가리키도록 바꿀 수 없다는 의미이다.

즉, 참조 타입 변수에 `final`을 붙였다고 해서 객체 내부 상태까지 불변이 되는 것은 아니다. 참조는 고정되지만, 객체의 필드가 변경 가능한 구조라면 내부 값은 바뀔 수 있다.

---

## 2. 클래스, 인스턴스, 생성자

클래스는 객체를 만들기 위한 설계도이고, 인스턴스는 그 클래스를 바탕으로 실제 메모리에 생성된 객체이다.

예를 들어 자동차라는 클래스를 기준으로 생각하면, 아반떼1, 아반떼2처럼 실제로 생성된 각각의 객체가 인스턴스이다.

생성자는 객체가 생성될 때 초기 상태를 설정하기 위해 사용된다. Java에서는 생성자 오버로딩을 통해 다양한 방식으로 객체를 생성할 수 있다. 같은 클래스라도 필요한 초기값에 따라 여러 생성자를 제공할 수 있고, 이를 통해 객체 생성 방식을 유연하게 만들 수 있다.

---

## 3. 싱글턴과 static 공유 상태

싱글턴은 프로그램 상에서 특정 객체가 하나만 생성되도록 보장해야 할 때 사용하는 객체 생성 방식이다.

이번 웹 서버 5단계에서는 세션 저장소를 구현하면서 `static`의 필요성을 이해했다. 현재 서버 구조에서는 요청마다 `RequestHandler` 객체가 새로 생성된다. 이때 세션 저장소를 인스턴스 필드로 두면, 요청마다 서로 다른 세션맵이 생긴다. 그러면 로그인 요청에서 저장한 세션 정보를 다음 요청에서 찾을 수 없게 된다.

따라서 세션 저장소는 모든 요청 핸들러가 공유해야 하므로 `static`으로 선언해야 한다. `static` 필드는 인스턴스가 아니라 클래스에 속하므로, 여러 `RequestHandler` 인스턴스가 생성되더라도 같은 세션 저장소를 바라볼 수 있다.

다만 이것은 싱글턴과 완전히 같은 개념은 아니다. `static` 상태와 `static` 메서드, `private` 생성자를 사용하면 정적 유틸리티 클래스 형태가 되고, 객체 하나를 만들어 공유하는 싱글턴과 구현 방식은 다르다. 하지만 전역적으로 하나의 상태를 공유한다는 점에서는 비슷한 효과를 낸다.

---

## 4. 다형성과 타입 변환

객체지향에서 다형성은 아직 헷갈리는 부분이다.

다형성은 부모 타입의 참조 변수로 자식 객체를 가리킬 수 있는 성질이다. 예를 들어 `Car car = new Avante()`처럼 선언하면, 실제 객체는 `Avante`이지만 변수 타입은 `Car`가 된다.

이 구조를 통해 같은 부모 타입으로 여러 자식 객체를 다룰 수 있다. 다만 이때 컴파일 시점에는 부모 타입에 정의된 메서드만 호출할 수 있고, 실제 실행 시점에는 오버라이딩된 자식 메서드가 실행된다.

즉, 다형성은 단순히 타입을 바꾸는 문법이 아니라, 부모 타입으로 여러 구현체를 통일해서 다룰 수 있게 해주는 객체지향의 핵심 개념이다. 아직 업캐스팅, 다운캐스팅, 오버라이딩과 연결되는 부분은 추가 복습이 필요하다.

---

## 5. 추상 클래스

추상 클래스는 상속을 전제로 한 미완성 클래스이다.

공통 기능은 부모 추상 클래스에 구현해두고, 일부 기능은 자식 클래스에서 반드시 구현하도록 강제할 수 있다. 즉, 추상 클래스는 “공통 뼈대는 제공하지만, 세부 구현은 자식에게 맡기는 구조”라고 이해할 수 있다.

이 개념은 여러 클래스가 공통된 흐름을 가지지만 일부 동작만 다를 때 유용하다.

---

## 6. 멀티스레드와 동기화

멀티스레드는 하나의 프로세스 안에서 여러 실행 흐름을 동시에 다룰 수 있도록 하는 프로그래밍 기법이다.

예를 들어 하나의 프로그램에서 파일 전송, 채팅, 화면 갱신 같은 작업을 동시에 처리하려면 여러 스레드를 사용할 수 있다. Java 프로그램은 기본적으로 `main` 메서드를 실행하는 메인 스레드에서 시작하고, 필요에 따라 작업 스레드를 추가로 만들 수 있다.

`Runnable`은 작업 스레드가 실행할 코드를 담는 객체이다. `Runnable`의 `run()` 메서드에 실행할 작업을 정의하고, 이를 스레드에 전달해서 실행할 수 있다.

멀티스레드 환경에서는 여러 스레드가 같은 자원에 동시에 접근할 수 있기 때문에 동기화 문제가 생긴다. 이를 제어하기 위해 `synchronized`를 사용할 수 있다. `synchronized`는 특정 영역이나 메서드에 한 번에 하나의 스레드만 접근하도록 제한해 데이터 불일치를 막는다.

이번 세션 구현에서는 여러 요청이 동시에 세션 저장소에 접근할 수 있으므로 일반 `HashMap`보다 `ConcurrentHashMap`이 적합하다는 점도 이해했다.

---

## 7. Java I/O 스트림

Java I/O에서는 `InputStream`과 `OutputStream`이 바이트 입출력의 최상위 스트림이다.

하위에는 여러 보조 스트림이 존재한다. 예를 들어 `BufferedInputStream`과 `BufferedOutputStream`은 내부 버퍼를 사용해 입출력 성능을 개선한다. 작은 단위로 여러 번 읽고 쓰는 것보다, 버퍼에 모아서 처리하면 더 효율적이다.

`DataInputStream`과 `DataOutputStream`은 primitive type 단위로 데이터를 읽고 쓸 수 있도록 도와주는 타입 맞춤형 스트림이다.

중요한 점은 보조 스트림은 단독으로 쓰는 것이 아니라, 기본 스트림에 감싸서 사용한다는 것이다. 예를 들어 파일 입력 스트림 위에 버퍼드 스트림을 붙이면, 실제 파일 입력 기능은 기본 스트림이 담당하고, 버퍼링 기능은 보조 스트림이 추가하는 구조가 된다.

---

## 8. HTTP의 stateless와 쿠키/세션

웹 서버 5단계에서는 쿠키와 세션을 이용해 로그인 상태를 유지하는 기능을 구현했다.

HTTP는 stateless 프로토콜이다. 즉, 서버는 각각의 요청을 독립적인 요청으로 처리하며, 이전 요청에서 누가 로그인했는지를 기본적으로 기억하지 못한다.

그래서 로그인 상태를 유지하려면 별도의 장치가 필요하다.

* 쿠키는 서버가 브라우저에 값을 저장시키고, 브라우저가 이후 요청마다 자동으로 다시 보내는 장치이다.
* 세션은 서버 쪽에 사용자 상태를 저장하는 방식이다.
* 일반적으로 쿠키에는 세션 식별자만 저장하고, 실제 사용자 정보는 서버의 세션 저장소에 보관한다.

로그인 성공 시 서버는 다음과 같은 응답 헤더를 내려준다.

```http
Set-Cookie: SID=abc; Path=/
```

그러면 브라우저는 이 쿠키를 저장하고, 이후 같은 서버로 요청할 때 다음과 같이 자동으로 쿠키를 보낸다.

```http
Cookie: SID=abc
```

`Set-Cookie`는 서버가 브라우저에게 쿠키를 저장하라고 보내는 응답 헤더이고, `Cookie`는 브라우저가 서버에게 자신이 가진 쿠키를 보내는 요청 헤더이다. 둘의 방향이 반대라는 점이 중요하다.

또한 응답의 `Set-Cookie`에는 `Path=/` 같은 속성이 포함될 수 있지만, 요청의 `Cookie` 헤더에는 보통 `key=value` 형태만 포함된다.

---

## 9. 웹 서버 5단계 구현 내용

이번 단계에서는 쿠키와 세션을 사용해 로그인 상태를 유지하는 기능을 구현했다.

구현한 주요 내용은 다음과 같다.

* `RequestHandler`

  * 요청 라우팅 처리
  * `POST /login`, `POST /logout` 분기 처리
  * 요청 쿠키를 읽어 로그인 상태 로깅

* `SessionManager`

  * `ConcurrentHashMap<SID, userId>` 기반의 인메모리 세션 저장소 구현
  * 로그인 성공 시 세션 생성
  * 요청 쿠키의 SID를 통해 사용자 식별

* `CookieParser`

  * 요청의 `Cookie` 헤더를 파싱
  * `SID=...` 형태의 쿠키 값을 추출

* `HttpResponses`

  * `Set-Cookie` 헤더를 포함할 수 있도록 응답 생성 로직 확장

* `login/index.html`

  * 로그인 폼의 `action`, `method`, `name`, `submit` 설정

이 과정에서 HTTP의 stateless 특성을 쿠키와 세션으로 보완하는 구조를 직접 구현해볼 수 있었다.

---

## 10. 라우팅 테이블과 Map.of

이번 구현에서는 URL과 실제 파일 경로를 매핑하기 위해 라우팅 테이블을 사용했다.

`PAGE_ROUTES` 같은 맵을 두면 `/index.html`, `/login.html` 같은 URL을 실제 정적 파일 경로와 연결할 수 있다. 이는 프레임워크에서 사용하는 `@RequestMapping`을 단순화해서 직접 구현한 것과 비슷하다.

라우팅 정보를 `if`문으로 하드코딩하는 것보다, 맵으로 관리하면 경로가 추가될 때 확장하기 쉽고 코드도 더 읽기 좋아진다.

`Map.of(...)`는 불변 맵을 만들 때 사용할 수 있다. 키와 값을 번갈아 넣는 구조이며, 반환된 맵은 수정할 수 없다. 따라서 변하지 않는 라우팅 테이블에는 적합하지만, 계속 추가·삭제가 발생하는 세션 저장소에는 적합하지 않다.

세션 저장소처럼 런타임에 계속 바뀌는 데이터는 `ConcurrentHashMap`처럼 가변적이고 멀티스레드에 안전한 자료구조를 사용해야 한다.

---

## 오늘의 핵심 정리

오늘 가장 중요한 학습은 Java의 메모리 구조와 웹 서버의 상태 관리가 실제 구현에서 연결된다는 점이었다.

기본 타입과 참조 타입, `final`, `static`, `ConcurrentHashMap` 같은 개념은 각각 따로 보면 문법처럼 보인다. 하지만 웹 서버에서 로그인 상태를 유지하려고 하자, 이 개념들이 실제 설계 판단으로 이어졌다.

특히 세션 저장소를 인스턴스 필드로 두면 요청마다 세션이 분리되어 로그인 유지가 되지 않고, `static`으로 공유해야 모든 요청이 같은 세션 저장소를 바라볼 수 있다는 점을 이해했다.

또한 HTTP는 stateless이기 때문에 서버가 사용자를 기억하려면 쿠키와 세션이 필요하다. 쿠키에는 세션 ID만 저장하고, 실제 로그인 정보는 서버의 세션 저장소에 보관하는 구조를 직접 구현하면서 웹 로그인 방식의 기본 원리를 학습했다.

한 줄로 정리하면, 오늘은 HTTP의 stateless 문제를 쿠키와 세션으로 해결하고, Java의 `static`, `final`, 참조 타입, 멀티스레드 동기화 개념이 웹 서버 구현에서 어떻게 쓰이는지 이해한 날이었다.

## 트랜잭션과 MySQL InnoDB 구조 정리

트랜잭션은 여러 데이터베이스 작업을 하나의 논리적 작업 단위로 묶은 것이다. 트랜잭션은 전체 작업이 모두 실행되거나 모두 취소되는 원자성, 트랜잭션 전후로 데이터 제약과 규칙을 유지하는 일관성, 동시에 실행되는 트랜잭션이 서로 간섭하지 않도록 하는 격리성, 커밋된 결과가 장애 이후에도 보존되는 영속성을 보장해야 한다.

격리성이 부족하면 Dirty Read, Non-repeatable Read, Phantom Read, Lost Update 등의 이상 현상이 발생할 수 있다. Dirty Read는 다른 트랜잭션의 미커밋 값을 읽는 현상이고, Non-repeatable Read는 동일한 행을 다시 조회했을 때 커밋된 수정으로 값이 달라지는 현상이다. Phantom Read는 동일한 조건으로 조회했을 때 행의 삽입이나 삭제로 결과 집합이 달라지는 현상이다.

SQL 표준의 격리 수준은 낮은 순서대로 READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, SERIALIZABLE이다. 격리 수준이 높을수록 이상 현상을 더 많이 방지하지만, 락 대기나 MVCC 버전 관리, 충돌에 따른 재시도 비용이 증가해 동시성과 처리량이 낮아질 수 있다.

MySQL은 SQL을 처리하는 서버 계층과 실제 데이터를 관리하는 스토리지 엔진 계층으로 나뉜다. 서버 계층은 연결과 인증, SQL 파싱, 실행 계획 최적화, 실행을 담당한다. 기본 스토리지 엔진인 InnoDB는 Buffer Pool, B+Tree 인덱스, 락, MVCC, Undo Log, Redo Log와 데이터 페이지를 관리한다. 두 계층은 일반적으로 하나의 `mysqld` 프로세스 안에서 실행된다.

InnoDB에서 테이블 데이터는 Primary Key 기준 클러스터드 B+Tree의 리프 노드에 저장된다. Primary Key 조회는 이 트리를 탐색해 실제 행을 찾는다. 보조 인덱스의 리프에는 보조 인덱스 값과 Primary Key가 저장되므로, 행 전체가 필요하면 Primary Key B+Tree를 다시 탐색한다. 적절한 인덱스가 없으면 클러스터드 인덱스의 리프 전체를 순회한다.

데이터를 수정하면 Buffer Pool의 페이지가 변경되어 Dirty Page가 된다. 변경 전 정보는 Undo Log에 기록되어 롤백과 MVCC의 과거 버전 조회에 사용되고, 변경 내용은 Redo Log에 기록되어 장애 복구에 사용된다. MVCC는 Read View와 Undo Log를 통해 트랜잭션에 적절한 행 버전을 제공하여 일반 조회와 쓰기의 충돌을 줄인다. 다만 `UPDATE`나 `SELECT FOR UPDATE`처럼 최신 데이터에 대한 정합성이 필요한 작업에는 여전히 락이 필요하다.

커밋은 트랜잭션의 변경을 최종 확정하는 작업이다. 커밋 시 변경된 데이터 페이지 전체를 즉시 디스크에 쓰는 것은 아니며, 복구 가능한 Redo Log를 먼저 안전하게 기록한다. 장애가 발생하면 Redo Log를 이용해 필요한 변경을 재적용하고, 커밋되지 않은 트랜잭션의 변경은 Undo Log를 이용해 취소한다.

InnoDB는 일반적으로 Steal과 No-force 정책을 사용한다. Steal은 미커밋 Dirty Page도 디스크에 기록할 수 있으므로 Undo가 필요하고, No-force는 커밋 시 데이터 페이지 전체를 즉시 기록하지 않으므로 Redo가 필요하다. 이러한 구조를 통해 디스크 쓰기 비용을 줄이면서 트랜잭션의 원자성과 영속성을 보장한다.

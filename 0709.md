7월 9일 일일 회고
오늘 학습한 내용

오늘은 웹서버 6단계를 진행하면서 클래스와 인터페이스, 함수형 인터페이스, 커넥션 풀, multipart 요청, PRG 패턴에 대해 학습했다.

1. 추상 클래스와 인터페이스

추상 클래스는 상속을 전제로 한 미완성 클래스라는 점을 다시 정리했다. 공통적으로 필요한 기능은 부모 추상 클래스에서 구현해두고, 세부 구현이 달라지는 메서드는 자식 클래스에게 위임하는 구조다.

인터페이스는 기능을 직접 구현하는 도구라기보다, 구현 클래스가 따라야 하는 계약서에 가깝다고 이해했다. 즉, 인터페이스는 “이 객체는 이런 기능을 제공해야 한다”는 규약을 정의하고, 실제 구현은 구현 클래스가 담당한다.

2. 함수형 인터페이스와 람다

오늘 가장 어려웠던 개념은 함수형 인터페이스였다.

함수형 인터페이스는 추상 메서드가 하나만 있는 인터페이스다. 메서드가 하나뿐이기 때문에 람다식을 전달했을 때 컴파일러가 해당 람다를 어떤 메서드의 구현으로 해석해야 하는지 명확하게 판단할 수 있다.

웹서버 개발 과정에서 POST 요청을 처리하는 핸들러들이 많았는데, 매개변수 구조가 비슷했기 때문에 이를 함수형 인터페이스로 선언해서 간단하게 처리할 수 있었다.

그 결과 요청마다 일일이 분기문을 작성하기보다, 요청 경로나 목적에 따라 핸들러를 매핑하는 방식으로 코드를 정리할 수 있었다.

오늘 이 부분을 통해 함수형 인터페이스는 단순히 람다 문법을 쓰기 위한 장치가 아니라, 동작 자체를 값처럼 전달하기 위한 구조라는 점을 이해했다.

3. 커넥션 풀

커넥션 풀은 DB와 이미 연결된 객체들을 미리 만들어두고, 요청이 들어올 때마다 빌려 쓰고 반납하는 구조다.

Connection 객체 자체는 JVM 힙 영역에 존재하지만, 내부적으로는 커널이 관리하는 TCP 소켓이나 파일 디스크립터 같은 OS 리소스를 참조한다. 즉, 단순한 자바 객체가 아니라 실제 DB 서버와 연결된 네트워크 리소스를 붙잡고 있는 객체다.

커넥션 풀을 사용하지 않으면 요청마다 TCP 연결 생성, DB 인증, DB 세션 생성, SQL 실행, 연결 종료 과정이 반복된다. 이 과정은 비용이 크기 때문에, 매번 새로 연결하지 않고 기존 연결을 재사용하는 것이 효율적이다.

다만 풀의 크기는 잘 관리해야 한다. 풀의 크기가 너무 작으면 요청이 커넥션을 기다리느라 지연될 수 있고, 너무 크면 DB 서버에 과도한 연결이 생겨 오히려 부하가 커질 수 있다.

따라서 커넥션 풀은 단순한 성능 최적화 도구가 아니라 서버와 DB 사이의 연결 수를 제어하는 장치이기도 하다.

4. Multipart 요청

multipart 요청에 대해서도 학습했다.

폼 태그에 enctype="multipart/form-data"를 명시하면 브라우저는 요청 바디를 단순한 key=value&key=value 형태가 아니라 여러 파트로 나누어 전송한다.

특히 이미지나 파일을 전송하는 경우에는 텍스트가 아니라 바이너리 데이터가 포함된다. 이 경우 기존처럼 BufferedReader로 문자 단위로 읽으면 문제가 생길 수 있다.

BufferedReader는 문자 스트림을 읽기 위한 도구이기 때문에, 이미지처럼 바이너리 데이터가 포함된 요청을 처리하기에는 적절하지 않다. 파일 업로드 요청에서는 InputStream이나 BufferedInputStream처럼 바이트 단위로 데이터를 읽는 방식이 필요하다.

이 내용을 통해 HTTP 요청 바디는 항상 같은 방식으로 읽으면 안 되고, Content-Type에 따라 처리 방식이 달라져야 한다는 점을 이해했다.

5. POST-Redirect-GET 패턴

POST 요청 처리 후 바로 결과 HTML을 응답하는 대신, 리다이렉트를 통해 GET 요청으로 전환하는 PRG 패턴도 학습했다.

기존 방식에서는 사용자가 POST 요청 후 새로고침하면 브라우저가 양식을 다시 제출하겠냐는 경고를 띄울 수 있다. 이는 새로고침 시 마지막 요청인 POST가 다시 실행될 수 있기 때문이다.

PRG 패턴은 POST 요청으로 데이터를 저장하거나 처리한 뒤, 서버가 302 Redirect 응답과 Location 헤더를 내려주고, 브라우저가 해당 경로로 다시 GET 요청을 보내는 흐름이다.

이렇게 하면 사용자가 결과 페이지에서 새로고침해도 다시 실행되는 것은 GET 요청이다. 따라서 중복 제출 문제를 줄이고, 사용자 입장에서도 양식 재제출 경고를 보지 않아도 된다.

웹서버 구현 관점에서는 단순히 200 OK만 반환하는 것이 아니라, 상황에 따라 302 Found와 Location 헤더를 적절히 내려주는 것이 중요하다는 점을 배웠다.

오늘의 핵심 정리

오늘 학습의 핵심은 웹서버가 단순히 요청을 받고 응답하는 프로그램이 아니라는 점이다.

요청의 형식, 데이터의 성격, 객체의 책임, DB 연결 관리, 사용자 경험까지 함께 고려해야 한다.

특히 오늘은 자바 문법과 웹서버 구현이 직접 연결되는 느낌이 강했다. 인터페이스와 함수형 인터페이스는 처음에는 추상적인 문법처럼 보였지만, 실제 웹서버에서 요청 핸들러를 분리하고 매핑하는 데 사용해보니 왜 필요한지 조금 더 명확해졌다.

또 커넥션 풀, multipart 요청, PRG 패턴은 Spring이나 Tomcat 같은 프레임워크를 사용하면 자동으로 처리되는 부분이지만, 직접 구현 흐름을 보면서 프레임워크가 내부에서 어떤 문제를 해결해주는지 이해할 수 있었다.

느낀 점

오늘은 기능 구현보다 구조를 이해하는 데 의미가 있었다.

특히 함수형 인터페이스를 사용하면서 요청 처리 로직을 객체처럼 넘기고 매핑할 수 있다는 점이 인상적이었다.

또 커넥션 풀을 통해 자바 객체가 단순히 힙에만 존재하는 것이 아니라, 내부적으로 커널의 소켓과 같은 외부 리소스를 참조할 수 있다는 점도 알게 되었다.

앞으로 웹서버를 구현할 때 단순히 코드가 동작하는지만 보지 않고, 이 기능은 어느 객체의 책임인지, 이 데이터는 문자로 읽어도 되는지 바이트로 읽어야 하는지, 요청 처리 후 어떤 상태 코드가 적절한지, 사용자가 새로고침했을 때 문제가 생기지 않는지, 프레임워크가 자동으로 해주던 일을 지금은 내가 직접 처리하고 있는지를 계속 생각하면서 개발해야겠다.

오늘 학습을 통해 웹 백엔드 개발은 단순한 API 구현이 아니라, HTTP, 객체지향, 리소스 관리, 사용자 경험을 함께 고려하는 작업이라는 점을 다시 확인했다.
